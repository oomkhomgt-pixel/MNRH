# แฟ้มสะสมงานแพทย์ประจำบ้านออร์โธปิดิกส์ — หน้าสาธิต

หน้าเว็บสาธิตสำหรับติดตามความก้าวหน้าการฝึกอบรมแพทย์ประจำบ้าน กลุ่มงานออร์โธปิดิกส์
โรงพยาบาลมหาราชนครราชสีมา

**เปิดที่:** https://oomkhomgt-pixel.github.io/MNRH/portfolio/

---

## หน้านี้ทำอะไร

ติดตามกิจกรรมตามหลักสูตรของแพทย์ประจำบ้านแต่ละคน โดยให้ **การบันทึกเกิดขึ้นตอนที่นำเสนอสไลด์**
แทนการมานั่งกรอกย้อนหลัง

แถบนำทางบนสุดเป็นสองชั้น: แถวบนคือ **กลุ่ม 6 กลุ่ม** — วันนี้ · ตาราง & ปฏิทิน · แพทย์ประจำบ้าน ·
ประเมิน (อาจารย์) · มาตรฐาน & ความครอบคลุม (อาจารย์) · ตั้งค่า & ข้อมูล (ผู้จัดหลักสูตร) —
กลุ่มที่มีหลายหน้าจะมีแถวหน้าย่อยโผล่ใต้แถวกลุ่ม (เช่น กลุ่มแพทย์ประจำบ้าน = รายชื่อ · แฟ้มรายบุคคล · Logbook ·
EPA · งานวิจัย — รายชื่อเป็นภาพรวมอย่างเดียว กด "เปิดแฟ้ม" จึงไปหน้าแฟ้มของคนนั้น ซึ่งมีตัวเลือกคนไว้สลับดู) ทุกหน้าที่เอกสารนี้อ้างถึงยังอยู่ครบ แค่ย้ายเข้าไปอยู่ในกลุ่ม

1. **รับสไลด์** — ลากไฟล์ `.pptx` หรือ `.pdf` ที่ใช้นำเสนอมาวาง หน้าเว็บจะอ่านไฟล์ในเครื่อง
   แล้วดึงชื่อเรื่อง ผู้จัดทำ วันที่สร้าง จำนวนสไลด์ และข้อความในทุกสไลด์ออกมา
2. **เดาให้ก่อน** — จากคำที่พบในสไลด์ ระบบเดา
   * **ประเภทกิจกรรม** (5 ประเภท): นำเสนอหัวข้อวิชาการ / Pre/post-op conference /
     Trauma film conference / Journal club / Research progression
   * **อนุสาขาออร์โธปิดิกส์** (8 อนุสาขา ใช้ชื่อภาษาอังกฤษ): Trauma, Spine, Arthroplasty, Sports,
     Hand, Foot & Ankle, Paediatric, Tumour
   * **ผู้นำเสนอ** จากชื่อผู้จัดทำที่ฝังอยู่ในไฟล์ เทียบกับรายชื่อแพทย์ประจำบ้าน
   พร้อมบอกว่ามั่นใจแค่ไหนและจับคำใดได้ ผู้บันทึกตรวจแล้วแก้ได้ทุกช่องก่อนกดยืนยัน
3. **เก็บเป็นหลักฐาน** — บันทึกชื่อไฟล์ จำนวนสไลด์ ข้อความที่อ่านได้ และลายนิ้วมือไฟล์ (SHA-256)
   ซึ่งใช้เตือนได้ด้วยเมื่อมีการนำไฟล์เดิมมาบันทึกซ้ำ
4. **สรุปให้เห็นส่วนขาด** — ความก้าวหน้าเทียบเกณฑ์รายชั้นปี ตารางความครอบคลุมอนุสาขารายบุคคล
   ตารางไขว้ประเภทงานนำเสนอ × อนุสาขา คลังหัวข้อที่เคยนำเสนอ ทะเบียนงานวิจัยรายขั้น
   และรายการที่ยังไม่ได้รับการรับรองจากอาจารย์
5. **ตาราง 2 แบบในหน้าเดียว** — หน้า “ตาราง: หมุนเวียน · เวร” มีการ์ด *ตอนนี้ใครอยู่หน่วยไหน*
   (หน่วยที่กำลังหมุนเวียน สถานที่ อาจารย์ผู้กำกับ วันสิ้นสุดบล็อก และเซสชันของวันนี้) พับไว้บนสุด กดหัวข้อเมื่ออยากดูรายคน
   เปิดหน้ามาจึงเจอตารางเลย แล้วสลับดูได้ 2 แบบ
   * **ตารางรายเดือน** — แถวคือแพทย์ประจำบ้าน คอลัมน์คือ 12 เดือนของปีการศึกษา **เริ่ม ก.ค. จบ มิ.ย.**
     ช่องระบายสีตามหน่วยที่หมุนเวียน: สายฟ้า สายขาว สายแดง สายเหลือง, หน่วยอนุสาขา (TR/PED, HND/ONC, FT/SPT, SPN, ARP),
     การวนนอกภาควิชา (ANES, RHEU, TRS, PM&R) และ ELE = free elective · ★ = หัวหน้าสายในเดือนนั้น (ปี 3 · ปี 4 เมื่อสายไม่มีปี 3)
     มีคำอธิบายสีกำกับ เลือกปีการศึกษาได้ กดที่ช่องเพื่อแก้ไขบล็อกนั้น พิมพ์และดาวน์โหลด CSV ได้
     · **ปีที่มีตารางจริงของกลุ่มงาน ใช้ตารางจริงก่อนเสมอ** — ปุ่ม “ใช้ตารางจริงของกลุ่มงาน” เป็นปุ่มหลักและอยู่หน้าสุด
       “สร้างแผนอัตโนมัติ” เป็นปุ่มรองที่**เติมเฉพาะคนที่ไม่อยู่ในตารางจริง** ไม่แตะแถวของตารางจริง (ถ้าทุกคนอยู่ในตารางจริงแล้ว
       กดไปก็ไม่มีอะไรเปลี่ยน ระบบบอกให้แก้รายช่องที่ตารางแทน) · ทั้งสองปุ่มเป็นสิทธิ์ของผู้จัดหลักสูตรเท่านั้น
     · ปุ่ม **“สร้างแผนอัตโนมัติ”** จัดแผนทั้งปีให้ตามกติกาด้านล่าง แล้วรายงานข้อที่ยังไม่ลงตัวให้ตรวจ — เขียนทับ
       เฉพาะปีการศึกษาที่เลือกไว้ (และในปีที่มีตารางจริง เขียนเฉพาะคนที่ไม่อยู่ในตารางนั้น)
       ก่อนเขียนทับทุกครั้งระบบจะกันสำเนาช่วงหมุนเวียนเดิมไว้ในเครื่องนี้อัตโนมัติ (สูงสุด
       10 ชุดล่าสุดต่อปี ไม่ซิงก์ขึ้นคลาวด์) กู้คืนได้จากปุ่ม **“ประวัติแผนอัตโนมัติ”** แม้จะรีเฟรชหน้าไปแล้วก็ตาม —
       คนละชั้นกับ toast “เลิกทำ” 6 วินาทีหลังกด ซึ่งยังมีไว้เหมือนเดิมสำหรับกรณีรู้ตัวทันที
     · ปุ่ม **“ใช้ตารางจริงของกลุ่มงาน”** โผล่เฉพาะปีการศึกษาที่มีตารางจริงฝังไว้ในระบบ (ตอนนี้: **2569** ทั้งปี 20 คน × 12 เดือน
       ถอดจากตารางของกลุ่มงาน) เปิดกล่องจับคู่แถวในตารางกับแพทย์ประจำบ้านในระบบ (เดาจากชื่อจริงให้ก่อน แก้ได้ทีละแถว
       เลือกคนซ้ำสองแถวไม่ได้) แล้วลงตารางทั้งปีแทนของเดิม — สำรองของเดิมไว้ใน “ประวัติแผนอัตโนมัติ” และเลิกทำได้เหมือน
       “สร้างแผนอัตโนมัติ” ใช้กู้ตารางจริงกลับมาได้ทันทีหลังเผลอกดสร้างแผน · ข้อมูลสาธิตของปี 2569 ก็มาจากตารางเดียวกันนี้
       (ตารางแสดง**ชื่อจริง**เสมอ ชื่อเล่นในตารางต้นฉบับไม่ถูกนำเข้าระบบ) · เครื่องที่มีข้อมูลเก่าอยู่แล้ว เปิดเวอร์ชันใหม่ครั้งแรก
       ระบบจะลงตารางจริงของปีที่ฝังไว้ให้เอง**หนึ่งครั้ง** (เมื่อจับคู่ชื่อได้ครบทุกแถวและตารางที่เก็บอยู่ต่างจากตารางจริง) พร้อมสำรองชุดเดิมและแจ้งให้รู้
       หลังจากนั้นไม่ทับที่แก้เองอีก · ยกเว้นกรณีเดียว: ถ้าปีนั้น**ว่างทั้งปี** (ล้างข้อมูลหรือนำเข้าไฟล์ที่ไม่มีตารางปีนี้)
       ระบบลงตารางจริงให้ใหม่เสมอ · ถ้าจับคู่ชื่อไม่ครบ ระบบไม่เดาและยังไม่ตั้งธง — แก้ทะเบียนชื่อให้ครบแล้วโหลดใหม่ ตารางจริงจะลงเอง
     · สี ตัวย่อ ประเภทหน่วย และ subspecialty ที่หน่วยครอบคลุม แก้ได้ในหน้าตั้งค่า
   * **เวรและคำขอ** — ผู้นิเทศ Topic conference รายสัปดาห์, คำขอแลกเวร OR ของอาจารย์ และคำขอไป attend activity —
     สามเรื่องที่ไม่ซ้ำกับตารางรายเดือนหรือช่วงหมุนเวียน รวมไว้ในแท็บเดียว (ดูรายละเอียดแต่ละคาบได้ที่การ์ด
     *ตอนนี้ใครอยู่หน่วยไหน* หรือหน้า *ปฏิทิน* แทน) · **ไม่มีตารางเวรรายเดือนให้กรอกแล้ว** — OPD/ER วันจันทร์–ศุกร์วนตามสาย
     ตามตารางประจำสัปดาห์อยู่แล้ว วันพฤหัสบดี (อาจารย์ออก OPD 1–2 คน และสาย ER ที่ไม่ซ้ำกับ OPD) กลุ่มงานจัดเองทุกเดือน
     กติกาเดิมคืออาจารย์ที่รับผิดชอบ ER เป็นผู้รับเคสใหม่และดูแล OR emergency · ข้อมูลเวร/วันหยุดที่เคยลงไว้ยังถูกอ่านตามเดิม
6. **ปฏิทินรวมการนำเสนอ** — หน้า “ปฏิทิน” แยกต่างหาก แสดง**เฉพาะการนำเสนอวิชาการ** (Trauma film conference,
   Pre/post-op conference, Topic conference, Journal club, Kahoot quiz) เป็นรายเดือน ชุดเดียวของทั้งภาควิชา —
   **อาจารย์/ผู้จัดหลักสูตรเห็นตารางรวมอย่างเดียว แพทย์ประจำบ้านเปิดมาเห็นทั้งกลุ่มงานก่อน แล้วสลับ “ของฉัน” ได้**
   * **Trauma film (F) และ Pre/post-op (P) ไม่ fix ผู้นำเสนอล่วงหน้า** — เป็นช่องประจำทุกวันราชการตามรูปแบบ morning
     conference (ค่าเริ่มต้น จันทร์ อังคาร พุธ ศุกร์ · F 09:00–09:30 ต่อด้วย P 09:30–10:00 · แก้วัน/เวลาได้ที่หน้าตั้งค่า
     การ์ด *รูปแบบ Morning conference*) วันข้างหน้าจึงเป็นช่องเปล่าเส้นประ “ยังไม่ระบุผู้นำเสนอ” ชื่อจะโผล่เมื่อมีสไลด์ส่งเข้าระบบ
     ในวันนั้น วันที่ผ่านไปแล้วโดยไม่มีสไลด์จะขึ้นจาง ๆ ว่า “ไม่มีสไลด์ส่ง” · วันหยุดราชการ (ตั้งในตารางเวรรายเดือน) ไม่มีช่อง
     · ช่องประจำไม่ใช่แถวข้อมูล คำนวณสดทุกครั้ง
   * **เช้าวันพฤหัสบดีเป็น 4 ช่วง ตามตารางจริงของกลุ่มงาน** (เวลาแต่ละช่วงตั้งได้ในการ์ดเดียวกับ morning conference):
     **ช่วงเปิด** เลือกได้ 4 แบบ — *Interesting Case Conference* (ค่าปกติ) · *Staff Lecture: หัวข้อ + อาจารย์* ·
     *Pediatric inter-hospital conference (D)* · หรือ **หัวข้อของ resident** เมื่อสัปดาห์นั้นมี 3 หัวข้อ (หัวข้อแรกมาอยู่ช่วงเปิด)
     สามแบบแรกเป็นของอาจารย์ ขึ้นปฏิทินเป็น C/L/D แสดงเฉย ๆ ไม่มีผู้นำเสนอ ไม่มีการประเมิน
     · **Topic ×2** (ปรับจำนวนได้ 0–3 ช่องต่อสัปดาห์ — บางสัปดาห์ของตารางจริงไม่มีหัวข้อเลย) แต่ละหัวข้อมี**ผู้นำเสนอ**
     (ปกติปี 1–2) + **chief ปี 3–4 ที่กำกับ** + **อาจารย์กำกับ**อีกชั้น — chip แสดง “ผู้นำเสนอ / chief”
     · **ช่วงปิด** *Journal club* (คนเดียว) หรือ **Kahoot quiz (K)** ที่**จัดร่วมกันกี่คนก็ได้ ชั้นปีใดก็ได้**
     (ตารางจริงมีทั้ง R3 สองคนและ R4 สามคน) · สัปดาห์ที่ยังไม่ลงตารางเห็นโครงเป็นช่องเปล่า T + J
     (กดลงตารางล่วงหน้าหรือ “ลง Kahoot quiz แทน” ได้)
     · **Topic/Journal club/Kahoot quiz ลงได้เฉพาะวันพฤหัสบดีเท่านั้น** — กล่องแก้ตารางนำเสนอปฏิเสธวันอื่นที่ช่องวันที่
     (แถวเก่าที่หลุดไปวันอื่นจากรุ่นก่อนหน้าถูกเก็บกวาดออกตอนโหลด กิจกรรมที่เคยผูกไว้ยังอยู่ในแฟ้มของเจ้าของเดิม แค่เลิกผูกกับตาราง)
   * **ตารางกิจกรรมจริงของกลุ่มงาน ก.ค.–ต.ค. 2569 ฝังมากับโปรแกรม** (`THURSDAY_PRESETS` + `EXTERNAL_CONF_PRESETS`)
     เครื่องที่มีข้อมูลอยู่แล้วได้ตารางนี้ตอนโหลดครั้งแรกหลังอัปเดต โดย**ลงเฉพาะสัปดาห์ที่ยังว่าง** ไม่ทับสัปดาห์ที่จัดเองแล้ว
     · ผู้จัดหลักสูตรสั่งลงซ้ำได้ที่ปุ่ม **“ลงตารางกิจกรรมจริง”** ในการ์ดผู้นิเทศ Topic conference (แท็บ “เวรและคำขอ”)
   * **งานเต็มเช้าวันพฤหัสบดี** — บางสัปดาห์ไม่มี 4 คาบเลย: **workshop (W)** และ **ประชุมร่วมต่างภาควิชา (M)** ของกลุ่มงานเอง
     (เช่น *Sport Physical Examination Workshop* 15 ต.ค. · *Ortho-Rehab-Anes Interdepartmental Conference* 29 ต.ค.
     ที่มีช่วงย่อยพร้อมเวลาและอาจารย์จาก Rehab/Anes) หรือ **การประชุมภายนอกที่ตั้ง “กินเวลาทั้งวัน”** (KOKU 2026 · RCOST)
     ซึ่งตัดทั้งวันรวมคาบประจำและกิจกรรมบังคับ · ทั้งสองแบบให้**ลงชื่อเข้าร่วม**ได้ ได้กิจกรรม *เข้าร่วมประชุม/workshop (A)*
     ในแฟ้มของตัวเอง — นับจำนวนได้ **ไม่มีแบบประเมิน** และไม่เข้าคิวประเมินของอาจารย์
   * **วันหยุดเฉพาะวัน** — วันพฤหัสบดีที่ตรงวันหยุดนักขัตฤกษ์ (เช่น 30 ก.ค. 2569 วันเข้าพรรษา) ติ๊ก “วันหยุดราชการ”
     พร้อมชื่อวันหยุดได้ในกล่องจัดการวันพฤหัสฯ — ตัดคาบประจำและกิจกรรมบังคับของวันนั้นทิ้ง (แถวที่ลงตารางไว้แล้วยังอยู่)
   * **ธีมประจำสัปดาห์** — ทุกหัวข้อของสัปดาห์อยู่ในธีมเดียวกัน วนสัปดาห์ละหนึ่งธีมตั้งแต่สัปดาห์แรกของรอบ (ค่าเริ่มต้น 6 ส.ค. 2569:
     Trauma → Spine → Metabolic bone → Shoulder & Sports → Hand → Foot & Ankle → Arthroplasty → Paediatric → Tumour · ธีมมีป้ายแยกจาก
     รหัสอนุสาขาที่ใช้ตั้งต้นหัวข้อ แก้ลำดับได้ในหน้าตั้งค่า) ป้ายธีมอยู่หัวช่องวันพฤหัสฯ รายการใหม่ตั้งต้นอนุสาขาให้ตามธีม
     กล่องรายละเอียดเตือนเมื่อหัวข้อออกนอกธีม · สัปดาห์ไหนสลับพิเศษหรือมี Staff lecture ผู้จัดหลักสูตร**กดป้ายธีม**
     (หรือปุ่ม “จัดการ” ในตารางผู้นิเทศ แท็บ “เวรและคำขอ”) เพื่อกำหนดธีมทับ ช่วงเปิด ผู้นิเทศ สายที่อยู่เวร
   * **แฟ้มของ chief และผู้จัด Kahoot** — เมื่อผู้นำเสนอ Topic บันทึกกิจกรรม (จากสไลด์หรือกดบันทึกเอง) ระบบสร้างกิจกรรม
     **“กำกับการนำเสนอ (chief)”** ให้ chief อัตโนมัติ ผูกกับแถวเดียวกัน (รออาจารย์รับรอง/ประเมินเหมือนกิจกรรมอื่น) · Kahoot quiz
     ผู้จัดร่วม**ทุกคน**ได้กิจกรรมประเภท *Kahoot quiz* คนละชุด · ทั้งสองประเภทไม่บังคับตามเกณฑ์ (0) แต่นับในตารางความครอบคลุมและประเมินได้
   * **ลงตารางล่วงหน้า** (วันที่ เวลา รายการ ผู้นำเสนอ chief/ผู้จัดร่วม หัวข้อ อนุสาขา สถานที่ อาจารย์กำกับ) — ผู้นำเสนอเว้นว่างไว้ก็ได้
     ระบบจะจับคู่คนที่ส่งสไลด์ประเภทนั้นวันใกล้กัน (ไม่เกิน 3 วัน) ให้เป็นผู้นำเสนอเอง · ฟอร์มเตือนเมื่อชั้นปีของผู้นำเสนอ/chief
     ต้องไปประชุมภายนอกวันนั้น
   * **การประชุมภายนอก (X)** — งานวิชาการนอกโรงพยาบาลที่ต้องไป (จากหัวตารางของกลุ่มงาน เช่น “20–21 Regional Hand: R4”)
     ผู้จัดหลักสูตรจัดการที่ปุ่ม “การประชุมภายนอก” หัวปฏิทิน (ชื่อ ช่วงวัน ชั้นปี **และรายคน** อนุสาขา สถานที่ และ “กินเวลาทั้งวัน”)
     — ตารางจริงระบุผู้ไปได้ทั้งแบบทั้งชั้นปีและรายคน (“R4 + R3 วิพุธ”) ขึ้นปฏิทินทุกวันในช่วง โผล่ในโหมด “ของฉัน” ของคนที่ต้องไป
     ซิงก์ข้ามเครื่องเหมือนตาราง · ไม่ใช่การนำเสนอ ไม่มีการประเมิน (งานที่กินเวลาทั้งวันลงชื่อเข้าร่วมได้)
   * **หัวเดือน** สรุปว่าเดือนนี้ใครวนนอกสาย (นอกภาควิชา / Elective / Subspecialty คำนวณจากช่วงหมุนเวียน) และการประชุมภายนอกของเดือน
   * **Inter-hospital conference บ่ายพฤหัสบดี (13:00–15:00)** เป็น**กิจกรรมบังคับเข้าทั้งรุ่น** (ค่าเริ่มต้นปี 4 — วัน เวลา
     และชั้นปีตั้งได้ในการ์ดเดียวกัน) ไม่ใช่การนำเสนอ: ไม่มีผู้นำเสนอ ไม่มีการประเมิน ขึ้นปฏิทินให้เห็นว่าวันนั้นทั้งชั้นปีต้องไป
     กดแล้วกล่องบอกวัน/เวลา และรายชื่อคนในชั้นปีที่ต้องเข้า · ในโหมด "ของฉัน" ของแพทย์ประจำบ้านจะโผล่เฉพาะคนที่อยู่ชั้นปีนั้น
     (คาบเข้าประชุมของแต่ละคนอยู่ในตารางส่วนกลาง/หน้า "วันนี้" อยู่แล้ว)
   * **หัวข้อประจำสัปดาห์ของ Inter-hospital conference ตลอดปีการศึกษา 2569** (`INTERHOSPITAL_PRESETS` ก.ค. 69 – มิ.ย. 70
     ตามตารางของกลุ่มงาน) ขึ้นต่อท้ายชื่อในชิป I เช่น “Inter-hospital conference: Spine · ปี 4 ทุกคน” — อ่านค่าคงที่นี้สด
     ทุกครั้งไม่ได้ก็อปปี้ลงข้อมูล ยังไม่มีหน้าจอแก้รายสัปดาห์ (แก้ที่ค่าคงที่โดยตรงถ้าต้องเปลี่ยน)
     · สัปดาห์ที่ตารางระบุ **Onsite** (ออกไปประชุมที่สถาบันกรุงเทพและปริมณฑล) ขึ้นข้อความต่อท้ายหัวข้อเป็นข้อมูลอย่างเดียว
     · สัปดาห์ที่ตารางระบุ **“ไม่มีการสอน”** เพราะบางคนไปประชุมภายนอกที่ชื่อเดียวกับหัวข้อ (TOSSM, AOTrauma, THKS, MBOG,
     SST, TOTAC, Annual Meeting) **ไม่ขึ้นชิป I** ของสัปดาห์นั้น (ไม่ใช่ทุกคนไปประชุม จึงไม่ใช่ภาคบังคับทั้งรุ่นอีกต่อไป)
     เหลือแค่ชิป **X** ของการประชุมนั้นให้เห็น — ผู้จัดหลักสูตรกรอกชั้นปี/รายชื่อผู้ไปจริงต่อการประชุมได้ทีหลังที่ปุ่ม
     “การประชุมภายนอก” (ตารางต้นฉบับไม่ได้ระบุมาว่าใครไปบ้าง) · วันหยุดราชการที่เหลือของปี (10/31 ธ.ค., 15 เม.ย., 20 พ.ค.,
     3 มิ.ย.) ลงเป็นวันหยุดใน `THURSDAY_PRESETS` เหมือน 30 ก.ค. ตัดคาบทั้งวัน
   * โหมด “ของฉัน” ของแพทย์ประจำบ้านรวมแถวที่ตนเป็น chief ผู้กำกับหรือผู้จัดร่วมด้วย
   * chip ในช่องวัน = **ตัวย่อประเภท + ชื่อผู้นำเสนอเต็ม** (Topic: “ผู้นำเสนอ / chief”) บรรทัดหนึ่ง และ**หัวข้อย่อ**บรรทัดสอง (ตัดด้วย …) สีขอบตามประเภท
     พื้นตามสถานะ (เขียว = บันทึกสไลด์แล้ว · ส้ม = ระบุคนแล้วแต่ยังไม่มีสไลด์ · ขาว = ตามกำหนด · เส้นประ = ช่องประจำยังไม่มีคน)
     มีคำอธิบายสีเหนือกริด จอแคบเหลือแต่ตัวย่อ
   * **กดที่รายการเพื่อดูรายละเอียดเต็ม** (ชื่อ ชั้นปี หัวข้อ อนุสาขา เวลา สถานที่ อาจารย์ผู้ดำเนินการ สถานะ และผู้นิเทศ/หัวหน้าสาย
     ของวันพฤหัสบดีนั้นสำหรับ T/J) ปุ่มท้ายกล่องขึ้นตามสิทธิ์: ผู้จัดหลักสูตรแก้ไขรายการได้ · เจ้าของแถว (หรืออาจารย์) ที่ยังไม่มี
     บันทึกสไลด์กดบันทึกว่านำเสนอแล้วได้เลย · รายการจากกิจกรรมเปิดกิจกรรมต่อได้เฉพาะคนที่มีสิทธิ์เห็นแฟ้มนั้น · ช่องประจำมีปุ่ม
     ไปหน้ารับสไลด์ และผู้จัดหลักสูตรลงตารางล่วงหน้าสำหรับวันนั้นได้
   * พิมพ์/CSV ให้ตามเดือนที่กำลังดู รวมทุกแหล่งเหมือนที่เห็นบนจอ (มีคอลัมน์ chief/ผู้จัดร่วม และธีมประจำสัปดาห์)
7. **รายละเอียดเซสชัน** — กดที่เซสชันใดก็ได้เพื่อดูข้อมูล (อาจารย์ผู้รับผิดชอบ สถานที่) บันทึกว่าวันนั้นเข้าคาบไหนเมื่อชนกัน
   หรือขอแลกวัน — ไม่มีแบบประเมินท้ายรายคาบอีกต่อไป การประเมินอยู่ที่**แบบประเมินการนำเสนอ** (Trauma film / Pre-post-op conference /
   Topic conference / Journal club) และ**แบบประเมินลงกอง**เมื่อจบรอบหมุนเวียนเท่านั้น
8. **พิมพ์ออกได้** — แฟ้มสะสมงานรายบุคคล (พร้อมช่องลงนาม) และรายงานหลักฐานตามมาตรฐาน WFME
   รวมถึงดาวน์โหลดเป็น CSV

## บัญชีผู้ใช้และสิทธิ์การเข้าถึง

เข้าใช้งานต้องล็อกอิน มี 3 บทบาท และเห็นข้อมูลเท่าที่จำเป็นตามบทบาท

| บทบาท | เห็นอะไร | ทำอะไรได้ |
|---|---|---|
| แพทย์ประจำบ้าน | แฟ้ม กิจกรรม logbook และ EPA **ของตนเองเท่านั้น** · ตารางหมุนเวียน/เวร/นำเสนอ ซึ่งเป็นข้อมูลส่วนรวม | บันทึกกิจกรรมของตนเอง ตั้งค่าซิงก์ของตนเอง · ประเมินไม่ได้ รับรองไม่ได้ |
| อาจารย์แพทย์ | ข้อมูลของแพทย์ประจำบ้านทุกคน เพื่อประเมินและรับรอง · หน้าความครอบคลุมและ WFME | ประเมินการนำเสนอ ประเมินลงกองสิ้นเดือน ประเมิน EPA รับรองกิจกรรม ระบุผู้ร่วมผ่าตัด นำเข้าเคส |
| ผู้จัดหลักสูตร | ทุกอย่าง | ทะเบียนผู้ใช้/อาจารย์/หน่วย ตารางหมุนเวียน เกณฑ์ หัวข้อ EPA การเชื่อมต่อ และการจัดการข้อมูล |

### อาจารย์แลกวัน OR กัน

อาจารย์นัดแลกวันกันเองเป็นครั้งคราว ตารางประจำสัปดาห์จึงไม่ตรงกับความจริงบางวัน

**ทางที่ผิดคือไปแก้ตารางประจำสัปดาห์** เพราะตารางนั้นเขียนครั้งเดียวใช้ทั้งปี แก้ทีเดียวสัปดาห์อื่นเปลี่ยนตามหมด
และคาบที่ผ่านไปแล้วจะกลายเป็นชื่ออาจารย์คนใหม่ย้อนหลัง ทั้งที่วันนั้นไม่ใช่เขา

**ทางที่ถูกคือบันทึกเป็นข้อยกเว้นของวันนั้น** ทับตารางเดิม — หลักการเดียวกับที่ตารางเวรรายเดือนใช้กับ ER
เก็บใน `store.data.swaps` แล้ว `applySwaps()` เปลี่ยนชื่อผู้รับผิดชอบตอนสร้างคาบของวันนั้น

#### การแลกเป็นคำขอ ไม่ใช่คำสั่ง

อาจารย์ฝ่ายหนึ่ง **ขอ** อีกฝ่าย **ตอบรับ** — ระหว่างรอตอบ ตารางยังเป็นชื่อเดิม เพราะยังไม่มีใครตกลงว่าจะมาแทน
`applySwaps()` มองเฉพาะรายการที่ `status === "accepted"` เท่านั้น

```
ขอ ──► pending ──► ตอบรับ ──► accepted ──► ตารางเปลี่ยน + สร้างอีกฝั่งของวันแลกกลับ
                └─► ปฏิเสธ ──► declined ──► ตารางไม่เปลี่ยน (เก็บไว้เป็นประวัติ)
```

| เรื่อง | เป็นแบบนี้ |
|---|---|
| ใครขอได้ | อาจารย์ขอได้เฉพาะคาบของตัวเอง · ผู้จัดหลักสูตรขอแทนได้ · แพทย์ประจำบ้านขอไม่ได้เลย |
| ใครตอบรับได้ | **เฉพาะฝ่ายที่ถูกขอให้มาแทน** หรือผู้จัดหลักสูตร — ผู้ขอกดตอบรับให้ตัวเองไม่ได้ |
| ระหว่างรอตอบ | ตารางยังเป็นชื่อเดิม และขึ้นป้ายว่ามีคำขอรออยู่ · ผู้ขอถอนคำขอเองได้ |
| ขอบเขต | เฉพาะวันที่ระบุ · เลือกได้ว่าทั้งวันหรือเฉพาะช่วง (เช้า/บ่าย/เต็มวัน/นอกเวลา) |
| การแสดงผล | ตอบรับแล้วขึ้นว่า **"นพ. ก (แทน นพ. ข)"** ไม่ใช่เปลี่ยนชื่อเงียบ ๆ |
| ผลประเมิน | ไม่ถูกย้าย — รหัสคาบ (`key`) ไม่มีชื่ออาจารย์อยู่ในนั้น การแลกจึงเปลี่ยนแค่ชื่อผู้รับผิดชอบ ไม่ได้เปลี่ยนตัวคาบ |
| แลกกลับ | ใส่วันที่แลกกลับไว้ อีกฝั่งถูกสร้าง **ตอนตอบรับ** ไม่ใช่ตอนขอ จึงเป็นการ **แลก** ไม่ใช่การ **ฝาก** |
| กันชนกัน | ถ้าคนที่ถูกขอมีคาบอื่นในวันนั้นอยู่แล้ว ระบบเตือนทั้งตอนขอและตอนตอบรับ |

คำขอที่รอตอบโผล่ที่หน้า **วันนี้** ของอาจารย์ที่ถูกขอ พร้อมปุ่มตอบรับ/ปฏิเสธ และเตือนถ้าวันนั้นเขาติดคาบอื่นอยู่

ขอได้สามทาง: ปุ่ม **+ ขอแลกวัน** ที่การ์ด "การแลกวัน OR ของอาจารย์" ในหน้าตาราง ·
ปุ่ม **ขอแลกวันนี้** บนการ์ดคาบของตัวเองในหน้า "วันนี้" · หรือเปิดคาบนั้นในปฏิทินแล้วกดจากในกล่อง

#### แพทย์ประจำบ้านไม่ย้ายตาม

แพทย์ประจำบ้านอยู่กับ **สายของตัวเองในเดือนนั้น** เสมอ — คนที่แลกคือคนลงมือผ่าตัด ไม่ใช่ทั้งทีม
การแลกวันจึงเปลี่ยนแค่ชื่ออาจารย์ที่มาคุมลิสต์นั้น ไม่ย้ายใครข้ามสาย มีข้อทดสอบยืนยันไว้

วันที่คนในสายไม่พอ **หัวหน้าสายเป็นคนตัดสินหน้างาน ไม่ได้กำหนดล่วงหน้า** ระบบจึงไม่มีช่องให้วางแผนเรื่องนี้ไว้ก่อน
แต่คำสั่งของหัวหน้าสาย**ไม่ใช่การอนุมัติ** — ดูหัวข้อถัดไป

### หน้าประเมิน — งานของอาจารย์ผู้คุมอยู่ที่เดียว

งานประเมินมีสองอย่างเท่านั้น รวมอยู่ในแท็บ **ประเมิน** เดียว (อาจารย์และผู้จัดหลักสูตรเท่านั้น) พร้อมตัวเลขค้างบนป้ายตั้งแต่ยังไม่กดเข้า
ไม่มีแบบประเมินท้ายเซสชันแยกต่างหากอีกแล้ว — คาบธรรมดา (ward round, OPD, OR) ไม่มีการให้คะแนน มีแค่บันทึกว่าเข้าคาบไหนและขอแลกวัน

| หน้าย่อย | คืออะไร |
|---|---|
| **งานนำเสนอ** | คิวงานที่ยังไม่ได้ให้คะแนนหรือยังไม่ได้กดรับรอง · กด “เปิด” เพื่อกรอกแบบประเมินการนำเสนอ และรับรอง |
| **ลงกองสิ้นเดือน** | หนึ่งแถวคือแพทย์ประจำบ้านหนึ่งคนที่วนสาย/อนุสาขานั้นตลอดเดือน · ประเมินหลังจบเดือน |

### pre-op กับ post-op conference เป็นกิจกรรมเดียวกัน

กลุ่มงานจัดคาบเดียว วางแผนเคสที่จะผ่าตัด แล้ววิจารณ์ผลของเคสที่ผ่าตัดไปแล้วในคราวเดียวกัน
ระบบจึงนับเป็นประเภทเดียว แสดงชื่อว่า **Pre/post-op conference** พร้อมคำอธิบายกำกับใต้ชื่อว่า
*วางแผนเคสที่จะผ่าตัด และวิจารณ์ผลของเคสที่ผ่าตัดไปแล้ว ในคาบเดียวกัน*
(`en` ที่ใช้ใน CSV คือ `Pre/Post-operative conference` แบบเต็ม) เหลือประเภทกิจกรรม 5 ประเภท

`id` ที่ใช้เก็บคือ `preop` เดิม ส่วน `postop` ถูกยุบเข้ามา — ข้อมูลเก่าถูกย้ายให้อัตโนมัติเมื่อเปิดหน้าเว็บครั้งถัดไป
ทั้ง**กิจกรรมที่บันทึกไว้** **รายการในตารางนำเสนอ** และ**เกณฑ์รายชั้นปี** ซึ่งเกณฑ์ของสองประเภทเดิมถูก**บวกรวมกัน**
ไม่ใช่ทิ้งไปข้างหนึ่ง (เช่น ปี 1 เดิม pre-op 10 + post-op 10 → 20) มีข้อทดสอบยืนยันทุกข้อ

### แบบประเมินการนำเสนอ

กลุ่มงานยังไม่มีฟอร์มเดิมสำหรับงานนำเสนอ ชุดนี้จึงเป็น**ร่างที่รออาจารย์ตรวจ** ไม่ใช่ของที่สรุปแล้ว

- **9 ข้อร่วม** แบ่งเป็นสามด้าน — *เนื้อหา* (ความถูกต้อง ความครบถ้วน การอ้างอิงหลักฐาน การประยุกต์)
  · *การนำเสนอ* (ลำดับและการคุมเวลา สไลด์ การสื่อสาร) · *การอภิปรายและความเป็นมืออาชีพ* (การตอบคำถาม ความเป็นมืออาชีพ)
  แบ่งเป็นด้านเพราะข้อเสนอแนะที่ใช้ได้จริงต้องบอกได้ว่าอ่อนตรงไหน ไม่ใช่ได้คะแนนรวมมาก้อนเดียวแล้วเดาเอง
- **1 ข้อเฉพาะประเภท** เพราะ conference คนละชนิดดูคนละเรื่อง — journal club ดูการวิจารณ์ระเบียบวิธีวิจัย,
  Pre/post-op conference ดูความสมเหตุสมผลของแผนผ่าตัดและการวิเคราะห์ภาวะแทรกซ้อนที่เกิดจริง,
  trauma film ดูการอ่านและแปลผลภาพรังสี, topic ดูความลึกของการทบทวนวรรณกรรม,
  ความก้าวหน้างานวิจัยดูความก้าวหน้าเทียบแผน · ให้ประเภทละหนึ่งข้อ ฟอร์มจะได้ไม่ยาวจนไม่มีใครกรอก
- สเกล **1–5** โดย **3 = ผ่านตามระดับชั้นปี** · ข้อที่ประเมินไม่ได้ให้เว้นว่าง ระบบไม่นับเป็นศูนย์
- **ผลการประเมินโดยรวม**: ผ่าน / ผ่านโดยมีข้อแนะนำ / ต้องนำเสนอซ้ำ
- แยก **สิ่งที่ทำได้ดี** ออกจาก **สิ่งที่ควรปรับปรุง** คนละช่อง — เขียนรวมช่องเดียวคนมักเขียนแต่คำชม

**การให้คะแนนบนหน้าจอ (ทั้งแบบประเมินการนำเสนอและแบบประเมินลงกอง)** — แต่ละข้อเป็น**ปุ่มคะแนน 1–5 แตะครั้งเดียว**
ไม่ใช่ dropdown ปุ่มแสดงเลขกับป้ายสั้น (เช่น "3 พอใช้") วางเมาส์เห็นประโยคเต็ม และมี "ความหมายของคะแนน 1–5" กางดูได้ที่ต้นฟอร์ม
· ปุ่ม **—** คือ "ไม่ได้สังเกต" ระบบไม่นับเป็นศูนย์ · ถ้าคะแนนที่บันทึกไว้เดิมไม่ตรงกับปุ่มใด (เช่น ทศนิยมจากการนำเข้า
หรือสเกลถูกแก้ขอบเขตภายหลัง) จะเห็นเป็นปุ่ม **"ค่าเดิม"** ติ๊กอยู่ — บันทึกซ้ำโดยไม่แตะจึงไม่ลบค่านั้นทิ้ง
· ปุ่มเป็น radio จริง จึงใช้คีย์บอร์ด (ลูกศร/Tab) ได้ · ทั้งสองฟอร์มเรียง 1→5 เหมือนกัน

**ขั้นตอนของอาจารย์เมื่อเปิดงานนำเสนอ** — แบบประเมินอยู่บนสุด ช่องแก้รายละเอียดกิจกรรม (ชื่อเรื่อง ประเภท อนุสาขา วันที่ ผู้นำเสนอ)
ยังอยู่ครบแต่พับไว้ใต้ "แก้ไขรายละเอียดกิจกรรม" (เจ้าของกิจกรรมที่ยังไม่มีผลประเมินเห็นช่องเหล่านี้ก่อนตามเดิม)
· ระบบโชว์**ค่าเฉลี่ยสด**และ**แนะนำผลโดยรวม**จากค่าเฉลี่ย (≥3.5 ผ่าน · ≥2.5 ผ่านโดยมีข้อแนะนำ · ต่ำกว่านั้นต้องนำเสนอซ้ำ)
เติมให้เฉพาะตอนที่อาจารย์ยังไม่ได้เลือกเอง เลือกเองเมื่อไรระบบเลิกเติมทับ
· **กด "อาจารย์รับรอง" กับฟอร์มว่างไม่ได้** — ต้องมีคะแนนหรือผลโดยรวมอย่างน้อยหนึ่งอย่าง (ข้อมูลสาธิตเดิมมีกิจกรรมที่รับรองไว้โดยไม่มีผลประเมิน
ซึ่งยังอยู่ตามเดิม กติกานี้กันเฉพาะการรับรองใหม่) · เปิดจากคิวของหน้าประเมินจะมีปุ่ม **"รับรอง แล้วเปิดถัดไป"** ไล่คิวได้โดยไม่ต้องกลับไปตาราง

**ขั้นตอนของอาจารย์เมื่อประเมินลงกอง** — หัวกล่องบอก**รอบก่อนหน้า** (เดือน สาย เฉลี่ย ผลโดยรวม) เพื่อให้คะแนนบนหลักฐาน
· ค่าเฉลี่ยสดกับผลโดยรวมที่แนะนำ (≥3.6 ผ่าน · ≥2.8 ผ่านโดยมีข้อแนะนำ ·
ต่ำกว่านั้นต้องติดตามเป็นพิเศษ) แนะนำเฉพาะเมื่อฟอร์มยังมีข้อ "ผลโดยรวม" ที่มีตัวเลือกค่านั้นและสเกลเป็น 1–5 · เปิดจากคิวมีปุ่ม
**"บันทึก แล้วเปิดคนถัดไป"** · ลบผลประเมิน (ผู้จัดหลักสูตร) **เลิกทำได้**จาก toast เหมือนการลบอย่างอื่นในแอป

`assessment.score` ยังเป็นค่าเฉลี่ยของข้อที่กรอก รายงาน แดชบอร์ด และกราฟที่อ่านค่านั้นอยู่จึงไม่กระทบ
CSV กิจกรรมมีคอลัมน์คะแนนรายข้อและผลการประเมินเพิ่มเข้ามา

**ประเมินลงกองสิ้นเดือน** คือสรุปทั้งเดือนของสาย/อนุสาขาที่วนอยู่ เก็บใน `rotationEvals` ผูกกับ `rotationId`
หนึ่งรอบต่อหนึ่งผล ผลที่บันทึกไว้ขึ้นในรายงานรายบุคคลที่สั่งพิมพ์ ในคอลัมน์ *ผลประเมินลงกอง*
ของตารางการหมุนเวียน และนับเป็นหลักฐาน WFME 3.1 / 3.2 / 7.1 / 7.2

**งานค้างเห็นได้โดยไม่ต้องไล่เดือน** — หน้า "วันนี้" ของอาจารย์มีการ์ด *ประเมินลงกองที่ค้าง* นับรอบที่จบในเดือนที่ปิดไปแล้วทุกเดือน
เฉพาะรอบของแพทย์ประจำบ้านที่ตนดูแล (รอบที่ระบุอาจารย์ผู้ดูแลเป็นคนอื่นไม่นับ) ไม่นับ external/elective
กดแถวใดก็เปิดหน้าประเมินที่เดือนนั้นให้ · หน้า "วันนี้" ของผู้จัดหลักสูตรมีตัวเลขค้างรวมทั้งกลุ่มงาน ·
ตัวเลือกเดือนในแท็บลงกองต่อท้ายด้วย "ค้าง n" ให้เห็นว่าเดือนไหนยังเหลือ

**แพทย์ประจำบ้านเห็นผลของตัวเองรายข้อ** — หน้ารายละเอียดมีตาราง *ผลประเมินลงกอง* (เดือน สาย เฉลี่ย ผลโดยรวมเป็นคำ
คะแนนรายข้อเป็นป้าย สิ่งที่ทำได้ดี สิ่งที่ควรพัฒนา) และเส้นเวลาการหมุนเวียนก็แสดงผลโดยรวมกับข้อความจากอาจารย์
รายงานพิมพ์แสดงผลโดยรวมเป็นคำภาษาไทย ไม่ใช่รหัสภายใน

### แบบประเมินลงกองแก้เองได้ ไม่ต้องแก้โค้ด

หัวข้อในแบบประเมินลงกองไม่ได้ฝังอยู่ในโปรแกรม แต่เก็บเป็นข้อมูลใน `store.data.rotationForm`
ที่ **ผู้จัดหลักสูตรแก้เองได้ที่หน้าตั้งค่า** เพราะฟอร์มจริงเป็นของกลุ่มงาน ไม่ใช่ของโปรแกรม
และถูกทบทวนทุกปี ชุดที่ให้มาเป็น**ค่าตั้งต้น** ไม่ใช่แบบประเมินทางการ

#### ชุดตั้งต้น 8 ข้อ — ประเมินการทำงานในสายรายเดือน

ครอบคลุมสมรรถนะ 6 ด้าน ด้านละ 1–2 ข้อ · สเกล 1–5 · **3 = ทำได้ตามระดับชั้นปี** · เว้นว่างได้ ไม่นับเป็นศูนย์
ตั้งใจให้สั้น เพราะของจริงคือ 20 คน × 12 เดือน ฟอร์มที่ยาวเกินไปจะได้ 3 หมดทุกช่องภายในเดือนที่สาม

| ข้อ | หมวด WFME ที่เป็นหลักฐานให้ |
|---|---|
| วางแผนการตรวจเพิ่มเติมและการรักษาได้เหมาะสมกับผู้ป่วยแต่ละราย | 2.3 · 2.6 |
| ทำหัตถการที่ชั้นปีนี้ควรทำได้ อย่างปลอดภัยและถูกขั้นตอน | 2.6 · 6.2 |
| ความรู้พื้นฐานและความรู้ของอนุสาขาที่วนอยู่ เพียงพอต่อการตัดสินใจ | 2.3 |
| รับข้อเสนอแนะแล้วนำไปปรับการทำงานจริงภายในรอบนี้ | 3.2 · 7.2 |
| ส่งเวรและปรึกษา/ส่งต่อข้ามทีมอย่างเหมาะสมเมื่อเกินขอบเขตของตนเอง ครบถ้วน ตรงเวลา ไม่ตกหล่น | 2.6 · 6.4 |
| ตรงต่อเวลา และรับผิดชอบงานที่ได้รับมอบหมายจนจบ | 1.2 · 4.5 |
| เคารพผู้ป่วย ญาติ และทุกวิชาชีพในทีม | 1.2 · 6.4 |
| ทำงานร่วมกับพยาบาล กายภาพ และสหวิชาชีพได้ราบรื่น | 6.4 |

ปิดท้ายด้วย ระดับ entrustment · ผลโดยรวม (ผ่าน / ผ่านโดยมีข้อแนะนำ / ต้องติดตามเป็นพิเศษ) ·
สิ่งที่ทำได้ดี · **สิ่งที่ควรพัฒนาในรอบหน้า** (ช่องหลังคือช่องที่ขึ้นในรายงานรายบุคคล)

**เรื่องการอ้างอิง WFME ที่ต้องเข้าใจให้ตรงกัน** — WFME เป็นมาตรฐานของ*หลักสูตร*
ไม่ใช่กรอบสมรรถนะสำหรับให้คะแนน*ตัวแพทย์ประจำบ้าน* หัวข้อที่ให้คะแนนจึงมาจากผลลัพธ์การเรียนรู้
(WFME 1.3 / 2.1) แล้ว**โยงกลับไปที่หมวดที่แต่ละข้อเป็นหลักฐานให้** ซึ่งเป็นวิธีเดียวกับที่ระบบทำกับ
กิจกรรมอยู่แล้ว · รหัสเหล่านี้เก็บอยู่ในฟิลด์ `wfme` ของแต่ละข้อ และ**หน้ามาตรฐาน WFME อ่านจากฟอร์มจริง**
ไม่ใช่รายการตายตัว — แก้ฟอร์มแล้วหน้ามาตรฐานเปลี่ยนตาม ไม่อ้างหลักฐานที่แบบประเมินไม่ได้ถามอีกแล้ว

ไฟล์ตัวอย่างพร้อมนำเข้า: [`docs/rotation-eval-form-wfme.csv`](docs/rotation-eval-form-wfme.csv)

| ชนิดข้อ | ตรงกับใน Google Form | เก็บที่ |
|---|---|---|
| `section` | หัวข้อคั่น | — (ไม่ต้องตอบ) |
| `scale` | Linear scale | `scores[id]` เข้าค่าเฉลี่ย |
| `choice` | Multiple choice / Dropdown | `scores[id]` ถ้าตั้ง *นับเป็นคะแนน* มิฉะนั้น `answers[id]` |
| `text` / `paragraph` | Short answer / Paragraph | `answers[id]` |
| `entrust` | — (สเกลกลางของระบบ) | `entrust` |

*ไม่รับ Checkboxes* เพราะคำตอบเป็นอาเรย์ ซึ่งพังทั้ง `scores` ทั้ง CSV หนึ่งช่องต่อข้อ และค่าเฉลี่ย
— ทำเป็น `choice` หลายข้อแทนได้ · *Grid* เขียนเป็น `section` + `scale` หลายแถวได้ครบโดยไม่เสียข้อมูล

**นำเข้า/ส่งออก CSV** คอลัมน์ `order, id, kind, question, options, minLabel, maxLabel, required, scored, wfme`
(`options` เขียนเป็น `1=ดีมาก|2=ดี|3=พอใช้` · `kind` รับทั้งชื่ออังกฤษและไทย เช่น `Linear scale`, `มาตราส่วน`)
ส่งออกไปแก้ใน Excel แล้วนำเข้ากลับได้ครบ · ถ้าไฟล์ไม่ได้ระบุว่าข้อไหนเป็นช่องข้อเสนอแนะ
ระบบใช้ข้อความยาว**ข้อสุดท้าย**ให้เอง เพราะช่องนั้นคือช่องที่รายงานรายบุคคลไปอ่าน

**กติกาที่ยึดไว้เสมอ — แก้ฟอร์มแล้วผลประเมินเดิมไม่หาย**
ถ้าลบข้อออกไป คำตอบที่บันทึกไว้แล้วยังอยู่ในเรกคอร์ด ขึ้นเป็นบล็อกเตือนอ่านอย่างเดียวท้ายใบประเมิน
และยังส่งออกไปกับ CSV ในคอลัมน์ *คำตอบข้ออื่นที่ไม่อยู่ในฟอร์มแล้ว* — ผลประเมินเป็นหลักฐานของคนที่ถูกประเมิน
การแก้ฟอร์มปีนี้ต้องไม่ลบของปีที่แล้ว

แต่ละผลประเมิน**ประทับสเกลที่ใช้ตอนนั้น** (`scaleMax`) ไว้ในตัวเอง เปลี่ยนสเกลเป็น 1–10 ปีหน้า
ผลเก่าจึงยังอ่านว่า `4.2/5` ไม่ใช่ `4.2/10` · ฟอร์มที่ไม่มีข้อให้คะแนนเลยจะขึ้นว่า *ประเมินแล้ว* แทนค่าเฉลี่ย

`rotationForm` ซิงก์ขึ้นคลาวด์เป็น **ก้อนเดียวทั้งชุด** (`WHOLE_KEYS`) ไม่ใช่รวมรายข้อ —
รวมรายข้อจะทำลายลำดับ และปล่อยให้สองเครื่องเอาข้อของตัวเองมาสลับกันจนได้ฟอร์มที่ไม่มีใครออกแบบ

**การอัปเกรดชุดตั้งต้น** ทำครั้งเดียวและทำเฉพาะฟอร์มที่ **ยังไม่เคยถูกแก้** (รหัสข้อตรงกับชุดตั้งต้นรุ่นก่อนเป๊ะ)
ฟอร์มที่กลุ่มงานแก้เองแล้วจะไม่ถูกทับไม่ว่ากรณีใด — ถ้าอยากได้ชุดใหม่ให้กด *คืนค่าตั้งต้น*
หรือนำเข้าไฟล์ CSV ข้างต้น

- ตัวกรอง **เฉพาะรอบที่ฉันเป็นอาจารย์ผู้กำกับ** ยังคงแสดงรอบที่*ไม่มี*ชื่ออาจารย์ผู้กำกับให้ทุกคนเห็น
  — ของที่ไม่มีเจ้าของคือของที่ไม่มีใครประเมิน ถ้าซ่อนก็คือซ่อนงานค้าง
- รอบที่ไปวนนอกกลุ่มงาน (external / free elective) ไม่อยู่ในคิว เพราะอาจารย์ของกลุ่มงานไม่ได้คุมเดือนนั้น
  หน้าจอบอกไว้ว่ามีกี่รอบและหน่วยอะไรบ้าง แทนที่จะเงียบไป

**แท็บ “กิจกรรมทั้งหมด” ถูกยุบเข้ามาเป็นหน้าย่อย “งานนำเสนอ”** ตัวกรองทั้งหก ช่องค้นหา และปุ่มดาวน์โหลด
CSV ตามที่กรอง ยังอยู่ครบ เพียงย้ายมาอยู่ในหน้าที่มันถูกใช้จริง แพทย์ประจำบ้านดูกิจกรรมของตัวเองได้ครบทุกรายการ
ที่หน้าแพทย์ประจำบ้าน (เดิมตัดไว้ที่ 40 รายการ) พร้อมปุ่ม *ดาวน์โหลดกิจกรรม (CSV)* ของตัวเอง

### ขอไป attend activity นอกสาย/นอกหน่วย — ต้องได้รับอนุมัติเสมอ

กติกาของกลุ่มงาน: **ทุกครั้งที่ไป attend activity นอกสาย/นอกหน่วยของตัวเอง ต้องได้รับอนุมัติ** ไม่มีข้อยกเว้น ไม่ว่าจะด้วยเหตุผลใด
(หัวหน้าสายมอบหมายหน้างานเพราะคนไม่พอ · เคสงานวิจัย · เคสที่หาดูยาก · ตามเก็บหัตถการ ฯลฯ)

คำขอ**ไม่ต้องเลือกว่าไปเข้ากิจกรรมอะไร** — ระบุแค่ **อาจารย์ที่รับผิดชอบเดิม** (ตั้งต้นให้จากคนที่คุมเขาวันนั้น) **อาจารย์ที่จะไป attend ด้วย**
ช่วง (ทั้งวัน/เช้า/บ่าย) และ**เหตุผล**เป็นข้อความ · อนุมัติแล้วเกิดคาบ *"Attend activity กับ อ. X"* ในตารางของเขา สังกัดหน่วยของอาจารย์คนนั้นในวันนั้น

**คำสั่งของหัวหน้าสายไม่ใช่การอนุมัติ** หัวหน้าสายเป็นแพทย์ประจำบ้านด้วยกัน มีอำนาจจัดคนหน้างาน
แต่การรับรองว่าใครปฏิบัติงานที่ไหนเป็นของอาจารย์ เพราะเป็นสิ่งที่เอาไปนับเป็นผลการฝึกอบรม
เรื่องที่เกิดไปแล้วขอย้อนหลังได้ ระบบกำกับป้าย **“ขอย้อนหลัง”** ไว้ให้ผู้อนุมัติเห็นว่ากำลังอนุมัติสิ่งที่เกิดไปแล้ว

```
แพทย์ประจำบ้านขอ ──► รออนุมัติ ──► อนุมัติ ──► คาบนั้นขึ้นในตาราง คาบของสายเดิมที่ชนกันกลายเป็นคาบที่ไม่ได้เข้า
                              └─► ไม่อนุมัติ ──► ตารางไม่ขยับเลย (เก็บไว้เป็นประวัติ)
```

`computeSessions()` สร้างคาบนี้เฉพาะคำขอที่ `status === "approved"` — **ก่อนอนุมัติ คาบนั้นไม่มีอยู่ในตารางเลย**
ระบบไม่บันทึกสิ่งที่ยังไม่ได้รับอนุญาต

| เรื่อง | เป็นแบบนี้ |
|---|---|
| ใครขอได้ | เจ้าของตารางเอง (หรือผู้จัดหลักสูตร) — ขอแทนคนอื่นไม่ได้ |
| **ใครอนุมัติได้** | **อาจารย์ที่รับผิดชอบเดิม · อาจารย์ที่จะไป attend ด้วย · อาจารย์ประจำสาย/หน่วยของเขาในเดือนนั้น** (ทุกคนที่มีชื่อในตารางประจำสัปดาห์ของสาย + อาจารย์ผู้กำกับบล็อก) — งานรวมของสายที่ไม่ใช่ OR อาจารย์ประจำสายคนใดคนหนึ่งรับทราบ/อนุมัติก็พอ · ผู้จัดหลักสูตรอนุมัติได้เสมอ · **ผู้ขออนุมัติให้ตัวเองไม่ได้** |
| ลำดับความสำคัญ | เท่ากับการเลือกเข้าคาบรายวัน (35) — ชนะงานบริการตามตาราง แต่ **ยังแพ้กิจกรรมวิชาการ (40)** เพราะวิชาการมาก่อนเสมอ |
| เหตุผล | ข้อความอิสระ ต้องใส่เสมอ |
| ขอย้อนหลัง | ทำได้ และติดป้ายไว้ให้ผู้อนุมัติเห็น — ไม่ใช่การแอบบันทึกทีหลัง |
| การแสดงผล | คาบนั้นติดป้าย **“ได้รับอนุมัติให้ไป attend activity”** พร้อมเหตุผล ไม่ใช่โผล่มาเฉย ๆ · คำขอรูปแบบเก่า (อ้างช่องในตารางของสายอื่น) ยังแสดงและสร้างคาบได้ตามเดิม |
| รายละเอียด | คาบนี้มีรหัสของตัวเอง เปิดดูรายละเอียด/ขอแลกวันได้ตามปกติ |

คำขอที่รออนุมัติโผล่ที่หน้า **วันนี้** ของอาจารย์ผู้อนุมัติ · แพทย์ประจำบ้านเห็นสถานะคำขอของตัวเองในหน้า **วันนี้** เช่นกัน

#### ทางลัดที่ปิดไว้

**การเข้าคาบรายวัน** (`sessionPicks`) บันทึกได้แค่ว่าวันนั้นเข้าคาบไหน *ในบรรดาคาบของตัวเอง* และระบุอาจารย์ได้
เฉพาะคนที่อยู่ในตารางของตัวเองวันนั้น — ระบุอาจารย์นอกสายตรงนั้นไม่ได้ ไม่งั้นมันจะกลายเป็นทางลัด
บันทึกการปฏิบัติงานนอกสายโดยไม่ผ่านการอนุมัติ มีข้อทดสอบยืนยันว่าทางลัดนี้ปิดจริง

### หน้า "วันนี้" — หน้าแรกสำหรับมือถือ

หน้าอื่นทั้งหมดตอบว่า "ภาพรวมเป็นอย่างไร" หน้านี้ตอบแค่ "ตอนนี้ฉันต้องไปไหน มีอะไรค้าง"
จึงไม่มีตารางใหญ่ เหลือเฉพาะสิ่งที่กดได้จากมือถือขณะยืนอยู่หน้าห้องผ่าตัด
เนื้อหาต่างกันตามบทบาท เพราะคำว่า "วันนี้" ของสามบทบาทไม่ใช่เรื่องเดียวกัน

| บทบาท | เห็นอะไร |
|---|---|
| แพทย์ประจำบ้าน | สายที่อยู่วันนี้ · คาบของตัวเองเรียงตามเวลา (คาบที่ระบุเวลามาก่อน แล้วค่อยคาบเต็มวัน) · เตือนเมื่อมีกิจกรรมวิชาการ · เตือนเมื่อมีคาบชนกันที่ต้องเลือกเอง · เคสห้องผ่าตัดของวันนั้นที่เกี่ยวกับหน่วยตัวเอง · กิจกรรมที่ยังรออาจารย์รับรอง |
| อาจารย์แพทย์ | คาบที่ระบุชื่อตัวเองไว้ พร้อมรายชื่อแพทย์ประจำบ้านในคาบนั้น กดประเมินได้ทันที · เซสชันของตัวเองที่ค้างประเมิน 14 วันย้อนหลัง · ประเมินลงกองที่ค้างของแพทย์ประจำบ้านที่ตนดูแล ทุกเดือนที่ปิดแล้ว |
| ผู้จัดหลักสูตร | วันนี้ใครอยู่สายไหน · เซสชันค้างประเมิน · ผลประเมินลงกองค้างทั้งกลุ่มงาน · กิจกรรมรอรับรอง · คนที่วันนี้ไม่มีคาบเลย · คนที่ยังไม่ได้กำหนดช่วงหมุนเวียน |

เดินวันย้อนหลัง/ไปข้างหน้าได้ด้วยปุ่ม ‹ › แต่เปิดหน้าใหม่ทีไรก็กลับมาเป็นวันนี้เสมอ
แพทย์ประจำบ้านและอาจารย์เข้าระบบแล้วมาถึงหน้านี้เป็นหน้าแรก ผู้จัดหลักสูตรยังเริ่มที่หน้ารับสไลด์ตามเดิม

### หน้าความครอบคลุม: ภาพรวมหนึ่งหน้า แล้วประเภทละหนึ่งหน้า

เดิมหน้านี้มีตัวกรองประเภทเป็นดรอปดาวน์ ซึ่งตอบได้ทีละคำถามและไม่บอกด้วยซ้ำว่ามีประเภทอะไรให้เลือกบ้าง
ตอนนี้เป็นแถบปุ่มที่เห็นทุกประเภทพร้อมจำนวนในวงเล็บตั้งแต่ยังไม่กด

**หน้าภาพรวม** — ตอบว่า *กลุ่มงานขาดอะไร*

| การ์ด | ตอบคำถามอะไร |
|---|---|
| แพทย์ประจำบ้าน × อนุสาขา | ใครยังไม่มีผลงานในอนุสาขาไหนเลย (นับรวมทุกประเภท) |
| **งานนำเสนอ ประเภท × อนุสาขา** | งานนำเสนอแต่ละประเภทกระจุกอยู่ที่อนุสาขาใด และอนุสาขาใดยังไม่เคยถูกหยิบมาทำ conference ชนิดนั้นเลย |
| หัวข้อที่เคยนำเสนอ | คลังหัวข้อทั้งหมด แยกตามอนุสาขา |

**หน้าของแต่ละประเภทงานนำเสนอ** (Pre/post-op conference, topic, Trauma film conference,
Journal club, Research progression) — ตอบว่า *ใครขาดประเภทนี้*

- ตัวเลขนำสายตา 4 ช่อง: นำเสนอไปแล้วกี่เรื่อง · ใครยังไม่เคยทำเลย · อนุสาขาไหนยังไม่ถูกหยิบมาทำ ·
  ใครยังไม่ถึงเกณฑ์ — เขียนชื่อไว้เป็นตัวหนังสือ ไม่ต้องไปไล่นับเอาจากตาราง
- ตาราง *แพทย์ประจำบ้าน × อนุสาขา* เฉพาะประเภทนั้น พร้อมคอลัมน์ **เกณฑ์** (เช่น `1 / 12`)
- คลังหัวข้อเฉพาะประเภทนั้น

**คอลัมน์เกณฑ์ขึ้นเฉพาะเมื่อเลือกปีการศึกษา** เพราะเกณฑ์ใน `requirements` เป็นเกณฑ์**รายชั้นปี**
เอายอดสะสมตลอดหลักสูตรไปเทียบจะได้ตัวเลขที่ดูแย่กว่าความจริง จึงตัดคอลัมน์นั้นทิ้งไปเลยแทนที่จะแสดงค่าที่ผิด

ข้อสังเกตเรื่องสีในตารางไขว้:

- ไล่**เฉดสีเดียว**จากอ่อนไปเข้ม เพราะช่องนี้วัด *ขนาด* ไม่ใช่แยก *ตัวตน*
  (ถ้าใช้หลายสีจะอ่านเป็นหมวดหมู่ ทั้งที่มันคือมาก–น้อย)
- **ช่องที่เป็นศูนย์ทำสีต่างหาก** เป็นแดงอ่อนพร้อมเครื่องหมาย ✕ ไม่ปล่อยให้จางหายไปกับพื้น
  เพราะศูนย์คือสิ่งที่เปิดตารางนี้มาหา
- ตารางไขว้**นับทุกประเภทเสมอ** เพราะถ้ากรองด้วยจะเหลือแถวเดียว ไม่เหลืออะไรให้อ่านไขว้

ทั้งหมดนี้อยู่ในแดชบอร์ดที่ส่งออกด้วย และวาดจากฟังก์ชันชุดเดียวกัน
(`typeSubTableHtml` · `covMatrixHtml` · `typeStatsHtml` · สไตล์ชุดเดียวใน `COV_CSS`)
ตัวเลขและหน้าตาบนจอกับในไฟล์จึงเพี้ยนจากกันไม่ได้ — มีข้อทดสอบยืนยันไว้

### แดชบอร์ดส่งออก

ปุ่ม **แดชบอร์ด** บนแถบบน (และในหน้าความครอบคลุม) ให้เลือก 2 รูปแบบ

- **สไลด์ (PPTX)** `dashboard-YYYY-MM-DD.pptx` — 16:9 **กราฟล้วน ไม่มีตารางตัวหนังสือ** (ชื่อเรื่อง + คำอธิบายบรรทัดเดียวต่อหน้า):
  ปกพร้อมตัวเลขหัวเรื่องและแถบสัดส่วน ครบ/เกินครึ่ง/ต่ำกว่าครึ่ง · ความก้าวหน้าเทียบเกณฑ์เป็นแถบต่อคนแยกชั้นปี (ตามหลังขึ้นก่อน) ·
  **Logbook รายคนรายปี** สามหน้า (ผู้ผ่าตัดหลัก / ผู้ช่วยผ่าตัดขึ้นไป / ผู้สังเกตการณ์ขึ้นไป) แถบต่อคนเทียบ**ขีดเกณฑ์สะสมของชั้นปี**
  (`เกณฑ์ › เคสผ่าตัด` ในหน้าตั้งค่า — ค่าตั้งต้นตรงกับตาราง RCOST ภาคผนวก 4 "ประสบการณ์การสะสมจำนวนหัตถการใน Electronic logbook")
  ตัวเลขเป็น**ยอดสะสมตลอดหลักสูตร**ไม่ใช่โควตาต่อปี และเคสขั้นสูงกว่านับแทนขั้นต่ำกว่าได้เสมอ (ผ่าตัดหลักนับเป็นทั้งผ่าตัดหลัก+ผู้ช่วย+ผู้สังเกตการณ์)
  เรียงคนที่ได้เคสน้อยสุดขึ้นก่อน แดง = ต่ำกว่าเกณฑ์ ส้ม = ต่ำกว่ามัธยฐานของรุ่น
  จะได้เห็นทันทีว่าใครต้องเร่งเก็บเคส · เคสแยกอนุสาขา (เข้ม = ผู้ผ่าตัดหลัก อ่อน = ผู้ช่วย) · กิจกรรมรายเดือนเป็นคอลัมน์ซ้อน ·
  EPA เป็นจุดค่าเฉลี่ยเทียบขีดเป้าหมาย · งานนำเสนอ ประเภท × อนุสาขา เป็นตารางสี — ทุกกราฟวาดด้วยรูปทรง แก้ต่อได้ทีละกล่อง
  เขียน Office Open XML ตรง ๆ (zip แบบเก็บ) **ไม่พึ่งไลบรารีภายนอก** ไฟล์เล็ก เปิดได้ใน PowerPoint / Keynote / Google Slides
- **ฉบับเต็ม (HTML)** `dashboard-YYYY-MM-DD.html` — ทุกตารางรวมภาคผนวก สำหรับพิมพ์/เก็บเข้าแฟ้ม

**ไฟล์เดียวจบ ไม่มีสคริปต์ ไม่มีไลบรารี ไม่อ้าง asset จากภายนอก** — เปิดได้แม้ไม่มีเน็ต
ส่งทางอีเมลได้ เก็บเข้าแฟ้มได้ และสั่งพิมพ์เป็น PDF ลง A4 แนวนอนได้ กราฟเป็น SVG เขียนตรง ๆ

| ส่วน | ตอบคำถามอะไร |
|---|---|
| ตัวเลขหัวเรื่อง | มีกี่คน กิจกรรมปีนี้เท่าไร เคสเท่าไร **กิจกรรมรออาจารย์รับรองกี่รายการ** |
| ความก้าวหน้าเทียบเกณฑ์ | **ใครตามหลังมากที่สุด** — หนึ่งแถวต่อคน เรียงแย่สุดขึ้นก่อน แยกตามชั้นปี |
| สิ่งที่ยังขาด | 10 คนแรกที่ต้องตาม พร้อมบอกว่าขาดอะไร |
| กิจกรรมรายเดือน | กิจกรรมกระจุกเดือนไหน แยกตามประเภท |
| เคสแยกอนุสาขา | อนุสาขาไหนมีเคสน้อย |
| EPA | หัวข้อไหนเฉลี่ยต่ำกว่าเป้าหมายของชั้นปี |
| **งานนำเสนอ ประเภท × อนุสาขา** | งานนำเสนอแต่ละประเภทกระจุกอยู่ที่อนุสาขาใด และอนุสาขาใดยังไม่เคยถูกหยิบมาทำ conference เลย |
| **ภาคผนวก ประเภทละหนึ่งหน้า** | หน้า A4 หนึ่งหน้าต่อประเภทงานนำเสนอ — ฉีกแจกคนที่ดูแล conference ชนิดนั้นได้เลย |
| ตารางตัวเลข | ตัวเลขชุดเดียวกับกราฟ สำหรับคนที่อยากอ่านค่าตรง ๆ |

**สิทธิ์:** ได้ข้อมูลเท่าที่บัญชีนั้นมองเห็น — แพทย์ประจำบ้านได้แดชบอร์ดของตัวเองคนเดียว
อาจารย์และผู้จัดหลักสูตรได้ทั้งกลุ่มงาน มีข้อทดสอบยืนยันว่าไม่มีชื่อคนอื่นหลุดเข้าไป

**ไม่มีข้อมูลผู้ป่วยรายบุคคลในไฟล์นี้** — เป็นตัวเลขรวมทั้งหมด ไม่มี HN ไม่มีชื่อ ไม่มีวันผ่าตัดรายเคส
จึงเป็นไฟล์ส่งออกชนิดเดียวที่ส่งต่อในที่ประชุมได้โดยไม่ต้องระวังเป็นพิเศษ

สีของกราฟชุดหมวดหมู่ผ่านการตรวจการมองเห็นสีบกพร่องแล้ว (ΔE ต่ำสุดของคู่ที่อยู่ติดกัน 9.1)
และทุกกราฟมีตัวเลขกำกับพร้อมตารางเดียวกันท้ายไฟล์ — สีจึงไม่ใช่ทางเดียวที่อ่านค่าได้

### แดชบอร์ดรายบุคคล

การส่งออกข้อมูลของแพทย์ประจำบ้านคนเดียว ได้แดชบอร์ดมาด้วยเสมอ ไม่ใช่ได้แต่ตาราง

- **หน้าแฟ้มสะสมงานที่สั่งพิมพ์** (ปุ่ม *พิมพ์แฟ้มสะสมงาน*) เปิดด้วยหน้า **ภาพรวมแบบเห็นเร็ว**
  ก่อนจะถึงตารางทั้ง 5–6 หัวข้อ อาจารย์ที่ปรึกษาที่ต้องเซ็นชื่อจึงเห็นสถานะได้ก่อนอ่านตาราง
- **ปุ่ม *ดาวน์โหลดแดชบอร์ด (HTML)*** บนการ์ดของแต่ละคน (และบนแถบหัวหน้าแพทย์ประจำบ้าน)
  ได้ไฟล์ `dashboard-<ชื่อ>-YYYY-MM-DD.html` ไฟล์เดียวจบ ไม่มีสคริปต์ ไม่อ้าง asset ภายนอก
  พิมพ์ลง A4 แนวนอนได้ประมาณ 3 หน้า

ทั้งสองที่วาดจากฟังก์ชันเดียวกัน (`residentDashboardHtml`) และใช้สไตล์ชุดเดียวกัน (`DASH_CSS`)
หน้าจอกับไฟล์จึงเพี้ยนจากกันไม่ได้

| ส่วน | ตอบคำถามอะไร |
|---|---|
| ตัวเลขหัวเรื่อง 5 ช่อง | ความก้าวหน้ารวมกี่ % · กิจกรรมปีนี้ · เคสที่เป็นผู้ผ่าตัดหลัก (สะสม) · อนุสาขาที่มีผลงาน · รอรับรอง |
| ความก้าวหน้าเทียบเกณฑ์ | แถบต่อหนึ่งเกณฑ์ พร้อมขีดดำที่ตำแหน่งเกณฑ์ของชั้นปี — **สีบอกว่าถึงแล้ว/เกินครึ่ง/ยังไม่ถึงครึ่ง** |
| สิ่งที่ยังขาด | รายการที่ต้องตาม เขียนเป็นประโยค ไม่ใช่ให้ไปนับเอาจากตาราง |
| อนุสาขาที่มีผลงานแล้ว | ป้าย 8 อนุสาขา เขียว = มีแล้ว แดง ✕ = ยังไม่มี |
| กิจกรรมรายเดือน | ทำงานกระจุกเดือนไหน แยกตามประเภท พร้อมตารางตัวเลขชุดเดียวกัน |
| เคสใน logbook | เคสกระจายในอนุสาขาไหน และแยกตามบทบาท |
| EPA | ระดับล่าสุดของแต่ละหัวข้อ เทียบเป้าหมายของชั้นปีตัวเอง (ไม่ใช่ค่าเฉลี่ยของกลุ่ม) |

**สิทธิ์:** ปุ่มนี้ผ่าน `canSeeResident` เหมือนทุกอย่างในระบบ แพทย์ประจำบ้านกดส่งออกของคนอื่นไม่ได้
แม้เรียกฟังก์ชันตรง ๆ และไฟล์ที่ได้ไม่มีชื่อแพทย์ประจำบ้านคนอื่นปนมาเลย ไม่มี HN ไม่มีข้อมูลผู้ป่วย
— มีข้อทดสอบยืนยันทุกข้อ รวมถึงข้อที่พิสูจน์ว่าตัวดักการดาวน์โหลดในเทสต์ทำงานจริง ไม่ใช่ผ่านฟรี

### ไอคอนแอป

**ใช้โลโก้จริงของกลุ่มงาน ไม่ได้วาดใหม่** — `portfolio/logo-lockup.png` คือต้นไม้พร้อมคำว่า **Ortho Korat**
ที่ตัดมาจากไฟล์โลโก้ตรง ๆ รูปทรง สี และตัวอักษรทุกจุดเป็นของเดิม

โครงเดียวกับไอคอนของระบบคิวห้องผ่าตัด: **โลโก้ด้านบน ชื่อระบบล่างกลาง**
ต่างกันตรงที่ชื่อของแฟ้มสะสมงานอยู่ใน **แถบทึบสีน้ำเงินเขียว** ส่วนของระบบคิวเป็นข้อความเปล่า

| แอป | ป้ายล่าง |
|---|---|
| แฟ้มสะสมงาน (แอปนี้) | `PORTFOLIO` ในแถบทึบ |
| ระบบคิวห้องผ่าตัด | `OR System` เป็นข้อความเปล่า |

เหตุผลที่ต้องมีแถบ ไม่ใช่แค่เปลี่ยนคำ: **ที่ 48px ตัวอักษรอ่านไม่ออกแล้วทั้งคู่**
ถ้าต่างกันแค่คำ สองไอคอนจะเหลือเป็นรอยเปื้อนเล็ก ๆ ใต้โลโก้เดียวกัน แยกไม่ออก
แถบทึบเป็นสิ่งเดียวที่ยังเห็นได้ทุกขนาด

สร้างไฟล์ใหม่ด้วย

```bash
CHROMIUM_PATH=/path/to/chrome node portfolio/docs/make-icons.mjs
```

สร้างครบทั้งสี่ใบจาก `logo-lockup.png` — เปลี่ยนคำบนแถบได้ที่ตัวแปร `LABEL` ในสคริปต์
`icon-maskable-512.png` ย่อทั้งก้อนเข้ามา 12% ให้ทั้งโลโก้และแถบรอดจากการครอปเป็นวงกลมและ squircle ของ Android

### รหัสสาธิตของแต่ละบทบาท

รหัสแยกตามบทบาท เพื่อให้สลับบทบาทแล้วเห็นว่าสิทธิ์ต่างกันจริง ไม่ใช่แค่เปลี่ยนชื่อที่มุมจอ

| บทบาท | บัญชี | รหัส |
|---|---|---|
| ผู้จัดหลักสูตร | `admin` | `9001` |
| อาจารย์แพทย์ | `staff1`–`staff23` | `2001`–`2023` |
| แพทย์ประจำบ้าน | `resident1`–`resident20` | `3001`–`3020` |

สูตรคือเลขนำหน้าตามบทบาท ต่อด้วยเลขบัญชีสองหลัก — `staff7` ใช้ `2007` · `resident12` ใช้ `3012`
ที่หน้าเข้าสู่ระบบมีปุ่มเติมรหัสให้ตอนสาธิต ข้อมูลเก่าที่ยังใช้รหัส `1234` จะถูกย้ายให้อัตโนมัติตอนเปิดครั้งถัดไป

**นี่คือรหัสสาธิต ไม่ใช่ระบบยืนยันตัวตน** การตรวจสิทธิ์ทั้งหมดทำในเบราว์เซอร์ ใครเปิด devtools ก็อ่านได้
ของจริงต้องผูกกับบัญชีของโรงพยาบาลและตรวจสิทธิ์ที่เซิร์ฟเวอร์

- ทุกการเข้าใช้ ประเมิน รับรอง นำเข้า และสร้างแผน ถูกบันทึกใน **ร่องรอยการใช้งาน** (500 รายการล่าสุด) ดูได้ที่หน้าตั้งค่า

### เส้นแบ่งสิทธิ์ที่บังคับไว้จริงในโค้ด

ข้อมูลทั้งภาควิชาอยู่ใน localStorage ก้อนเดียวกันของเครื่องที่ดึงข้อมูลจากคลาวด์มา
การกรองตามบทบาทจึงต้องครบทุกทาง ไม่ใช่เฉพาะหน้าที่นึกออก จุดที่บังคับไว้มีดังนี้

| ทาง | ใครทำได้ | บังคับที่ |
|---|---|---|
| ส่ง/ดึง **ข้อมูลทั้งชุด** ขึ้น-ลงคลาวด์ | ผู้จัดหลักสูตร | `cloudPush()` / `cloudPull()` และ `syncMode()` ที่ตกกลับเป็นโหมด logbook ให้บัญชีอื่น |
| ตั้งที่อยู่ปลายทางการซิงก์ | ผู้จัดหลักสูตร | `syncSettingsDialog()` — บัญชีอื่นได้หน้าอ่านอย่างเดียวที่แสดงเฉพาะรายการของตนเอง |
| ส่งออก/นำเข้าไฟล์ข้อมูลทั้งชุด | ผู้จัดหลักสูตร | `exportJson()` / `importJson()` + `data-perm="admin"` ที่ปุ่ม |
| เปิด/แก้กิจกรรม | เจ้าของกิจกรรม และอาจารย์ | `openActivity()` · `applyActivityEdits()` · `savePending()` |
| ย้ายกิจกรรมไปเป็นของคนอื่น | ผู้จัดหลักสูตร | `applyActivityEdits()` |
| ให้คะแนน/ข้อเสนอแนะ | อาจารย์ | `applyActivityEdits()` — เจ้าของกิจกรรมแก้คะแนนตัวเองไม่ได้ |
| ติ๊ก "อาจารย์รับรอง" ในเคสผ่าตัด | อาจารย์ | `editCaseParticipants()` — ช่องถูกปิดและค่าเดิมถูกคงไว้แม้แก้ DOM ตรง ๆ |
| ระบุบทบาทของตัวเองในเคส | เจ้าของแถวนั้น และอาจารย์ | `editCaseParticipants()` — แถวที่แก้ไม่ได้จะถูกคงไว้ทั้งแถว ไม่เขียนทับกันข้ามคน |
| ลบเคสออกจาก logbook | ผู้จัดหลักสูตร | `editCaseParticipants()` |
| แก้ทะเบียนแพทย์ประจำบ้าน / บัญชีผู้ใช้ | ผู้จัดหลักสูตร | `editResident()` / `editUser()` |

- **logbook เป็นของรายบุคคลเสมอ** — ถ้ายังไม่ได้ระบุว่าเป็นของใคร `syncPayloadItems()` จะส่งรายการเปล่า
  (เดิมช่องว่างแปลว่า "ทุกเคสในเครื่อง" ซึ่งเผลอส่งเคสของคนอื่นออกไปได้)
- **ข้อความจากสไลด์ถูกล้างก่อนเก็บ** — `scrubPatientText()` ตัดสิ่งที่ดูเหมือน HN เลขบัตรประชาชน
  เลขเวชระเบียน และชื่อที่ขึ้นต้นด้วยคำนำหน้าออก ก่อนบันทึกเป็นหลักฐานของกิจกรรม
  (การเดาประเภท/อนุสาขาทำจากข้อความเต็มไปแล้วก่อนถึงขั้นนี้ จึงไม่กระทบความแม่น)
- **ไฟล์สำรองคือฐานข้อมูลทั้งก้อน** รวมข้อมูลผู้ป่วยใน logbook และรหัสผ่านของบัญชี
  เก็บในดิสก์ของภาควิชาเท่านั้น ไม่ส่งต่อทางอีเมลหรือแชท — หน้าเว็บถามยืนยันก่อนดาวน์โหลดทุกครั้ง
- **ข้อจำกัดที่ต้องรู้**: การตรวจสิทธิ์ทั้งหมดอยู่ในเบราว์เซอร์ จึงเป็นการ *แบ่งมุมมองและกันแก้ผิดคน* บนเครื่องที่ใช้ร่วมกัน
  **ยังไม่ใช่การยืนยันตัวตนเชิงความปลอดภัย** — ของจริงต้องผูกกับบัญชีของโรงพยาบาลและตรวจสิทธิ์ที่เซิร์ฟเวอร์
  โครงนี้แยกชั้นไว้แล้ว (`currentUser()` / `canSeeResident()` / `canAssess()` / `canManage()`) จึงเปลี่ยนไปใช้ session
  ของเซิร์ฟเวอร์ได้โดยไม่ต้องรื้อหน้าอื่น

## การประเมิน EPA

- หน้า **EPA** แสดงหัวข้อแยกตามอนุสาขา พร้อม **ระดับล่าสุด** เทียบ **เป้าหมายของชั้นปีนั้น** และประวัติการประเมินย้อนหลัง
- อาจารย์กดประเมินได้จากในหน้า: เลือกระดับ 1–5 (ชุดเดียวกับ entrustment ท้ายเซสชัน) ผูกกับเคสผ่าตัดได้ และใส่ข้อสังเกต
- อาจารย์เห็นตารางภาพรวมทั้งกลุ่มงาน (แพทย์ประจำบ้าน × หัวข้อ) สีเขียวคือถึงเป้าหมายของชั้นปีแล้ว
- ผู้จัดหลักสูตรแก้หัวข้อและตั้งเป้าหมายรายชั้นปีได้ที่หน้าตั้งค่า

> **หัวข้อ EPA ที่ใส่มาให้ตอนนี้เป็นตัวอย่างสำหรับวางโครงเท่านั้น ยังไม่ใช่รายการจริง**
> เมื่อได้รายการจริงของกลุ่มงาน ให้นำเข้าทับได้ทันที — CSV คอลัมน์ `code, title, subspecialty, year1..year4`
> หรือ JSON `[{code, title, subspecialty, targets:{1..4}}]` (ส่งออกไฟล์ตัวอย่างจากปุ่มข้าง ๆ ได้)
> การนำเข้าจะแทนที่หัวข้อเดิมทั้งชุด และล้างผลประเมินที่อ้างหัวข้อเก่าออกไปด้วย

## ใช้เป็นแอปบนมือถือ และเป็นเว็บแอปพร้อมกัน

หน้านี้เป็น **PWA** แล้ว — โค้ดชุดเดียวใช้ได้ทั้งบนเบราว์เซอร์และติดตั้งเป็นแอปบนมือถือ

| อุปกรณ์ | วิธีติดตั้ง |
|---|---|
| Android (Chrome/Edge) | กดปุ่ม **“ติดตั้งแอป”** มุมขวาบน หรือเมนู ⋮ → ติดตั้งแอป |
| iPhone / iPad (Safari) | ปุ่มแชร์ → **เพิ่มไปยังหน้าจอโฮม** (iOS ไม่มีปุ่มติดตั้งอัตโนมัติ) |
| คอมพิวเตอร์ | ใช้ในเบราว์เซอร์ได้เลย หรือกดไอคอนติดตั้งในแถบที่อยู่ |

เมื่อติดตั้งแล้วจะเปิดเต็มจอ มีไอคอนของตัวเอง และ **ใช้งานได้แม้ไม่มีสัญญาณ** เพราะหน้าเว็บถูกเก็บไว้ในเครื่อง
ด้วย service worker และข้อมูลอยู่ใน localStorage อยู่แล้ว · เงื่อนไข: ต้องเสิร์ฟผ่าน **https** หรือ **localhost**
(เปิดไฟล์ตรง ๆ แบบ `file://` จะติดตั้งไม่ได้) · หน้า “ตั้งค่า &amp; ข้อมูล” มีปุ่ม **ตรวจการเชื่อมต่อ**
ที่บอกสถานะการติดตั้ง การทำงานออฟไลน์ และสาเหตุเมื่อต่อระบบคิวไม่ได้

**ถ้าต้องการเป็นแอปใน store หรือแจกผ่าน MDM ของโรงพยาบาลจริง ๆ** ห่อไฟล์ชุดนี้ด้วย Capacitor ได้โดยไม่ต้องเขียนใหม่

```bash
npm create @capacitor/app@latest ortho-portfolio     # แล้วคัดลอกไฟล์ในโฟลเดอร์นี้ไปไว้ใน www/
npm i @capacitor/android @capacitor/ios
npx cap add android && npx cap add ios
npx cap sync && npx cap open android                 # build เป็น .apk / .ipa ต่อจากนี้
```

ข้อดีที่ได้เพิ่มจากการห่อเป็นแอป native คือคำขอเครือข่ายวิ่งผ่าน native HTTP layer จึง**ไม่ติดกฎ CORS และ
mixed content ของเบราว์เซอร์** ซึ่งเป็นอุปสรรคหลักเมื่อระบบคิวอยู่ใน intranet (ดูตารางถัดไป)

## เชื่อมกับระบบคิวที่อยู่ใน intranet ได้ไหม

ได้ แต่ผลลัพธ์ขึ้นกับว่า **หน้านี้ถูกวางไว้ที่ไหน** เพราะเบราว์เซอร์บังคับกฎ 2 ข้อ: หน้า https เรียก http ไม่ได้
(mixed content) และการเรียกข้ามโดเมนต้องได้รับอนุญาตจากปลายทาง (CORS)

| วางหน้า portfolio ไว้ที่ | ดึงเคสจากระบบคิว | ข้อควรรู้ |
|---|---|---|
| **เซิร์ฟเวอร์เดียวกับระบบคิวใน intranet** (แนะนำที่สุด) เช่น `https://or-queue.hospital.local/portfolio/` | ได้ทันที เรียก `/api/cases` แบบ same-origin | ไม่ต้องตั้ง CORS ไม่ติด mixed content และใช้ session ที่ผู้ใช้ล็อกอินระบบคิวไว้แล้ว |
| เครื่องแม่ข่ายอื่นใน intranet ที่เป็น **https** | ได้ ถ้าระบบคิวส่ง `Access-Control-Allow-Origin` เป็นโดเมนนี้ และ `Access-Control-Allow-Credentials: true` | ต้องแก้ตั้งค่าฝั่งระบบคิว |
| เครื่องแม่ข่ายอื่นใน intranet ที่เป็น **http** | ได้ ถ้าหน้านี้ก็เป็น http เหมือนกัน | แต่ http จะติดตั้งเป็นแอป (service worker) ไม่ได้ |
| **GitHub Pages** (https สาธารณะ) | **เรียกระบบคิวที่เป็น http ใน intranet ไม่ได้** เบราว์เซอร์บล็อกเพราะ mixed content | ใช้ทางนำเข้าไฟล์ JSON/CSV แทน หรือย้ายไปโฮสต์ใน intranet |
| **แอป native (Capacitor)** | เรียกตรงได้ ข้าม CORS/mixed content | เครื่องต้องอยู่ในวง intranet หรือ VPN ของโรงพยาบาล |
| ไม่เชื่อมเลย | export JSON/CSV จากระบบคิว → นำเข้าที่หน้า Logbook | ใช้ได้เสมอ ไม่ต้องแก้อะไรฝั่งระบบคิว |

**ทดสอบโดยไม่ต้องต่อ intranet**: มี [ตัวจำลอง API ของระบบคิว](../or-queue-mock/) อยู่ในที่เก็บนี้
เปิดด้วย `node or-queue-mock/server.js` แล้วตั้งที่อยู่ API เป็น `http://127.0.0.1:8088`
เคสที่สร้างอิงตารางจริงของกลุ่มงาน (ศัลยแพทย์ อนุสาขา วันผ่าตัด) การเดาผู้ร่วมผ่าตัดจึงทำงานเหมือนของจริง
และจำลองอาการเสียได้ด้วย (`?_fail=500|401|empty|garbage|slow`) เพื่อดูว่าหน้าเว็บบอกสาเหตุถูกหรือไม่

สรุปที่ใช้ได้จริงที่สุดสำหรับสถานการณ์ “ระบบคิวเป็นเว็บแอปใน intranet”: **วางไฟล์หน้านี้ไว้บนเซิร์ฟเวอร์เดียวกับระบบคิว**
แล้วให้แพทย์ประจำบ้านติดตั้งจากที่อยู่ intranet นั้นลงมือถือ เวลาอยู่ในโรงพยาบาล (ต่อ wifi โรงพยาบาลหรือ VPN)
จะดึงเคสได้ตรง ๆ ส่วนเวลาอยู่นอกโรงพยาบาลยังเปิดแอปดูและบันทึกได้ตามปกติ เพียงแต่ดึงเคสใหม่ไม่ได้จนกว่าจะกลับเข้าวง

หน้าตั้งค่าการเชื่อมต่อมีสวิตช์ **“ไม่บันทึก HN ลงเครื่องนี้”** สำหรับกรณีติดตั้งบนโทรศัพท์ส่วนตัว
เมื่อเปิดไว้ HN จะไม่ถูกเก็บลงเครื่องเลย (ทั้งข้อมูลเดิมและที่นำเข้าใหม่)

## ควรวางระบบอย่างไรเมื่อ OR queue อยู่ใน intranet แต่ผู้ใช้ต้องใช้จากนอกโรงพยาบาล

ข้อเสนอ: **แยกเป็นสองโซน** และให้ intranet ถูกใช้เฉพาะตอนดึงข้อมูลเคสเท่านั้น

- **โซนนอก** — แฟ้มสะสมงานสำหรับแพทย์ประจำบ้านและอาจารย์ (ติดตั้งเป็นแอปได้ ใช้จากที่บ้านได้ ไม่ต้อง VPN)
- **โซนใน** — ระบบคิวห้องผ่าตัดอยู่ที่เดิม ไม่ต้องเปิดออกสู่อินเทอร์เน็ต

ตัวเชื่อมระหว่างสองโซนเลือกได้ 3 แบบ เรียงจากที่แนะนำ

1. **ให้ผู้ใช้ดึงเองตอนอยู่ในโรงพยาบาล** (ทำได้แล้ววันนี้ ไม่ต้องแก้อะไรฝั่งระบบคิว)
   เปิดแอปตอนต่อ wifi โรงพยาบาล กดดึงเคส ระบบเก็บเฉพาะข้อมูลที่ตั้งระดับไว้ · ไม่ต้องเปิดพอร์ตใด ๆ ออกนอก
   ข้อจำกัดคือต้องอยู่ในวงและต้องกดเอง
2. **สคริปต์ซิงก์ขาเดียวจากข้างใน** (แนะนำถ้าจะทำระบบกลาง) — โปรแกรมเล็ก ๆ รันในโรงพยาบาล ดึงเคสจากระบบคิว
   → ตัดข้อมูลผู้ป่วยออก → ส่งขึ้นเซิร์ฟเวอร์ของแฟ้มสะสมงานผ่าน HTTPS **ขาออกอย่างเดียว**
   ไม่ต้องเปิด inbound ให้ intranet เลย และแพทย์ประจำบ้านเห็นเคสได้โดยไม่ต้องอยู่ในโรงพยาบาล
3. **เปิดระบบคิวออกนอกผ่าน VPN หรือ reverse proxy** — ไม่แนะนำ เพราะเพิ่มพื้นที่เสี่ยงให้ระบบที่มีข้อมูลผู้ป่วยจริง

**สิ่งที่ควรระวังเรื่อง PDPA** ถ้าเซิร์ฟเวอร์ของแฟ้มสะสมงานอยู่นอกโรงพยาบาล

- อย่านำ **ชื่อผู้ป่วยและ HN** ออกไป — ใช้ระดับ “ไม่เก็บข้อมูลผู้ป่วยเลย” ในหน้าตั้งค่าการเชื่อมต่อ
  (เก็บเฉพาะ diagnosis / operation / บทบาท / วันที่) แล้วอ้างอิงกลับด้วยเลขที่เคสของระบบคิวซึ่งไม่มีความหมายนอกโรงพยาบาล
- ถ้าจำเป็นต้องมี HN เพื่อตรวจสอบย้อนกลับ ให้เก็บไว้เฉพาะในเครื่องผู้ใช้หรือในวง intranet เท่านั้น
- ระบบกลางควรมี: ล็อกอินผูกกับบัญชีโรงพยาบาล, สิทธิ์ตามบทบาท (ผู้เข้าฝึกอบรม / อาจารย์ / ผู้จัดหลักสูตร),
  บันทึกการเข้าถึง, การสำรองข้อมูล, กำหนดอายุการเก็บ และเอกสาร ROPA/DPIA ตามที่โรงพยาบาลใช้อยู่

**ลำดับที่แนะนำให้ทำ**

1. *ตอนนี้* — ใช้แบบไฟล์เดียวต่อเครื่องไปก่อน ดึงเคสตอนอยู่ในโรงพยาบาล พิสูจน์ว่ากระบวนการเวิร์กกับกลุ่มงานจริง
2. *ขั้นถัดไป* — เพิ่มเซิร์ฟเวอร์กลางเล็ก ๆ (ผู้ใช้ + สิทธิ์ + ซิงก์) วางใน DMZ ของโรงพยาบาล ผูกกับบัญชีเดิม
   ทั้งแอปอ่านเขียนผ่านชั้น `store` ชั้นเดียว จึงต่อซิงก์ได้โดยไม่ต้องรื้อหน้าอื่น
3. *เมื่อพร้อม* — เพิ่มสคริปต์ซิงก์ขาเดียวจากข้างใน เพื่อให้ logbook เต็มโดยไม่ต้องรอให้ผู้ใช้กดเอง

## ซิงก์ logbook ขึ้นระบบกลางอัตโนมัติ

หน้า Logbook มีปุ่ม **“ซิงก์ระบบกลาง”** ตั้งที่อยู่ปลายทาง เลือกว่าเป็น logbook ของใคร แล้วระบบจะส่งให้เอง —
ส่งตอนเปิดแอป ตอนมีการแก้ไข (รวบไว้ 5 วินาทีแล้วส่งครั้งเดียว) ทุก 10 นาทีถ้ายังค้าง และทันทีที่กลับมาออนไลน์
มีป้ายสถานะบนแถบบน (ซิงก์แล้ว / รอซิงก์ / กำลังซิงก์)

**ข้อมูลที่ส่งออกมีเพียง 3 อย่าง: วันที่ผ่าตัด, diagnosis, operation** (บวก `role` ถ้าผู้ใช้เลือกเปิด)
ไม่มี HN ไม่มีชื่อผู้ป่วย ไม่มีอายุ/เพศ ไม่มีชื่อศัลยแพทย์ · หน้าตั้งค่าจะแสดง **ตัวอย่าง JSON จริงที่จะถูกส่ง**
ให้ตรวจก่อนเปิดใช้

สัญญาข้อมูล: `PUT {url}` รับ `{user, updatedAt, items[]}` และ `GET {url}?user=…` คืน `{items[]}`
มีเซิร์ฟเวอร์อ้างอิงพร้อมทดลองอยู่ที่ [`sync-server/`](../sync-server/) ซึ่ง **ตัดฟิลด์ที่ไม่ได้ตกลงกันทิ้งอีกชั้นหนึ่ง**
ก่อนบันทึก ของจริงควรเพิ่มเส้นทางนี้เข้าไปในเซิร์ฟเวอร์ของระบบคิวที่มีระบบผู้ใช้อยู่แล้ว

## ฐานข้อมูลกลาง 2 ทาง: คลาวด์ของภาควิชา + ดิสก์ของภาควิชา

**ทางที่ 1 — คลาวด์ของภาควิชา (ฐานข้อมูลกลางที่ใช้งานร่วมกัน)**

- เลือกโหมด **“ฐานข้อมูลกลางของภาควิชา — ส่งข้อมูลทั้งชุด”** ในหน้าซิงก์ แล้วใส่ที่อยู่คลาวด์
- ส่งขึ้นอัตโนมัติเหมือนเดิม (ตอนเปิดแอป · 5 วินาทีหลังแก้ไข · ทุก 10 นาทีถ้าค้าง · เมื่อกลับมาออนไลน์)
- ส่งทั้งชุด: ทะเบียนแพทย์ประจำบ้าน/อาจารย์/หน่วย ตารางหมุนเวียน เซสชันและผลประเมิน กิจกรรม งานวิจัย logbook EPA เกณฑ์ และข้อมูลหลักสูตร
  · **ไม่ส่งรหัสผ่านของบัญชี** และเมื่อดึงกลับมา รหัสเดิมในเครื่องยังใช้ล็อกอินได้
- ปุ่ม **“ดึงข้อมูลทั้งชุดจากคลาวด์”** ใช้ตอนตั้งเครื่องใหม่หรือกู้คืน โดยจะบอกก่อนว่าจะทับด้วยข้อมูลชุดไหน อัปเดตล่าสุดเมื่อไร จากเครื่องใด
- ฝั่งเซิร์ฟเวอร์อ้างอิงเก็บ **เวอร์ชันย้อนหลัง 10 รุ่น** (`GET …/dataset?versions=1` ดูรายการได้)

**ทางที่ 2 — สำรองลงดิสก์ของภาควิชา**

- ที่หน้าตั้งค่า เลือกโฟลเดอร์บนดิสก์ของภาควิชาไว้ครั้งเดียว (Chrome/Edge บนคอมพิวเตอร์) ระบบจำสิทธิ์ไว้ให้
- กด **“สำรองลงโฟลเดอร์ตอนนี้”** หรือปล่อยให้สำรองเองเมื่อถึงกำหนด (ตั้งได้ว่ากี่วันครั้ง) — ชื่อไฟล์ตามวันเวลา
  หมุนไฟล์เก่าออกตามจำนวนที่ตั้งไว้ และมีประวัติการสำรองให้ตรวจ
- ถ้าเบราว์เซอร์เขียนโฟลเดอร์ไม่ได้ (Safari, มือถือ) ใช้ปุ่มดาวน์โหลดแล้วเก็บไฟล์เองได้ ระบบยังนับเป็นการสำรองและบันทึกประวัติให้
- มีคำเตือนเมื่อเลยกำหนดสำรอง และกู้คืนจากไฟล์ได้จากปุ่มเดียวกัน

**ข้อควรทำเมื่อใช้จริง**

- ยึดหลัก 3-2-1: ข้อมูลหลักบนคลาวด์ · สำเนาบนดิสก์ภาควิชา · และควรมีอีกชุดเก็บคนละที่ตั้ง
- **ทดสอบกู้คืนจริงเป็นระยะ** อย่างน้อยปีละครั้ง — ไฟล์สำรองที่ไม่เคยลองกู้คืนไม่นับว่าเป็นการสำรอง
- ดิสก์ที่ใช้สำรองควรเข้ารหัสทั้งลูก และบัญชีคลาวด์ควรมีผู้ดูแลมากกว่าหนึ่งคน เผื่อคนเดียวลาออกหรือติดต่อไม่ได้
- ไฟล์สำรองมีข้อมูลทั้งหมดที่เครื่องนั้นเก็บ รวมถึง logbook — ถ้าตั้งระดับข้อมูลผู้ป่วยเป็น “เก็บ HN” ไฟล์สำรองก็จะมี HN ด้วย
  ให้เลือกระดับให้ตรงกับที่ตกลงกับโรงพยาบาล และเก็บไฟล์สำรองในที่ที่คุมการเข้าถึงได้
### กันการแก้พร้อมกันทับกัน

ทำไว้ 3 ชั้น ทำงานร่วมกัน

1. **เซิร์ฟเวอร์นับรุ่น (revision) และปฏิเสธของที่ล้าสมัย** — ทุกครั้งที่ส่งขึ้น เครื่องต้องบอกว่าแก้มาจากรุ่นไหน
   ถ้ามีคนอื่นส่งขึ้นไปก่อนจนรุ่นไม่ตรง เซิร์ฟเวอร์ตอบ **409 พร้อมส่งของปัจจุบันกลับมา** แทนที่จะยอมให้เขียนทับ
2. **รวมข้อมูลแบบสามทางที่ฝั่งเครื่อง** — แต่ละเครื่องเก็บ *ฐาน* ไว้ = ชุดข้อมูลตอนซิงก์สำเร็จครั้งล่าสุด
   เมื่อชนกันจึงเทียบได้ว่า **อะไรคือสิ่งที่เครื่องนี้แก้จริง** แล้วเอาเฉพาะส่วนนั้นไปวางทับบนของคลาวด์
   รายการที่เราไม่ได้แตะจะไม่ทับของคนอื่น จากนั้นส่งใหม่อัตโนมัติด้วยรุ่นล่าสุด
   · การลบก็เทียบกับฐานเช่นกัน ของที่เราลบจะไม่ถูกปลุกกลับมา และถ้าเครื่องอื่นเพิ่งแก้รายการที่เราลบ ระบบจะคงของเขาไว้แล้วบันทึกไว้ให้ตรวจ
3. **บันทึกรายการที่ชนกันจริง** — เฉพาะกรณีที่ *รายการเดียวกัน* ถูกแก้ทั้งสองฝั่ง ระบบจะเก็บฉบับของเครื่องที่ส่งทีหลัง
   แล้วลงรายการไว้ที่หน้าตั้งค่า (ส่วนข้อมูล / รายการ / เวลา / ผลที่เลือก) เพื่อให้คนตรวจย้อนได้ ไม่ใช่หายเงียบ

ผลที่ได้ (ทดสอบด้วยสองเครื่องพร้อมกัน)

| สถานการณ์ | ผลลัพธ์ |
|---|---|
| สองเครื่องแก้ **คนละรายการ** | รวมกันได้ทั้งคู่ ไม่มีอะไรหาย ไม่ถือเป็นการชนกัน |
| สองเครื่องแก้ **รายการเดียวกัน** | เก็บฉบับของเครื่องที่ส่งทีหลัง + บันทึกรายการที่ชนกันไว้ให้ตรวจ |
| เครื่องหนึ่งลบ อีกเครื่องแก้รายการอื่น | การลบมีผล และการแก้ของอีกเครื่องยังอยู่ |
| เครื่องหนึ่งลบ อีกเครื่องแก้ **รายการที่ถูกลบ** | คงของที่ถูกแก้ไว้ก่อน แล้วบันทึกไว้ให้คนตัดสิน |

**ลดการชนกันตั้งแต่ต้นด้วยการแบ่งความเป็นเจ้าของ** — สิทธิ์ที่ตั้งไว้ช่วยอยู่แล้ว: ทะเบียนและตารางแก้ได้เฉพาะผู้จัดหลักสูตร
ผลประเมินเป็นของอาจารย์ผู้ประเมิน และบันทึกของแพทย์ประจำบ้านแต่ละคนแยกกัน โอกาสที่สองคนแก้ *รายการเดียวกัน* พร้อมกันจึงเหลือน้อยมาก

**ยังไม่ได้ทำ (ควรทำเมื่อขึ้นระบบจริง)** — รวมข้อมูลระดับ *ฟิลด์* ในรายการเดียวกัน, ส่งเฉพาะรายการที่เปลี่ยน (delta) แทนทั้งชุด,
ล็อกเชิงแนะนำเวลาแก้ตารางทั้งผืน และเก็บประวัติการแก้รายรายการไว้ที่เซิร์ฟเวอร์เพื่อย้อนกลับได้ทีละรายการ

## ความสัมพันธ์กับระบบ logbook ของราชวิทยาลัย

ระบบของราชวิทยาลัยเป็น **ที่บันทึกอย่างเป็นทางการ** และต้องแนบฟิล์มกับ operative note ซึ่งเจ้าตัวต้องลงเอง
หน้านี้จึง **ไม่ทำ logbook ซ้อน** และไม่เก็บฟิล์มหรือ op note

บทบาทของหน้านี้คือ **ตัวเตรียมข้อมูลและตัวจับตก**

- ดึงเคสจากระบบคิวมาให้ครบ แล้วระบุบทบาทของแต่ละคนในไม่กี่คลิก (ข้อมูลที่ระบบคิวมีอยู่แล้วไม่ต้องพิมพ์ซ้ำ)
- ทำเครื่องหมาย **“ลงระบบราชวิทยาลัยแล้ว”** รายเคส เพื่อให้เห็นว่าเหลือค้างกี่เคส — มีตัวกรอง “ยังไม่ได้ลงราชวิทยาลัย”
  และปุ่มทำเครื่องหมายทีเดียวทั้งชุดที่กรองไว้
- พิมพ์ **“ใบกรอกราชวิทยาลัย”** เฉพาะเคสที่ยังไม่ได้ลง เรียงตามวันที่ พร้อมช่องติ๊ก เพื่อนั่งกรอกรวดเดียว
- นับจำนวนเคสตามบทบาทเข้าเกณฑ์ของหลักสูตร และใช้เป็นหลักฐาน WFME ในภาพรวมของกลุ่มงาน

ถ้าวันหนึ่งราชวิทยาลัยเปิด API ให้ส่งข้อมูลเข้าโดยตรง ก็ต่อเพิ่มจากจุดนี้ได้ทันที เพราะข้อมูลถูกจัดรูปไว้แล้ว

## ข้อมูลยังแยกกันคนละเครื่อง

ต้องรู้ก่อนตัดสินใจ: ตอนนี้ข้อมูลเก็บใน **เบราว์เซอร์ของแต่ละเครื่อง** ไม่ได้แชร์กัน — แพทย์ประจำบ้านบันทึกในมือถือตัวเอง
อาจารย์เปิดในคอมพิวเตอร์จะไม่เห็นข้อมูลนั้น ย้ายข้อมูลได้ด้วยไฟล์สำรอง JSON เท่านั้น

ถ้าต้องการให้ทุกคนเห็นข้อมูลชุดเดียวกัน (แพทย์ประจำบ้านบันทึก อาจารย์รับรอง ผู้จัดหลักสูตรดูภาพรวม)
ต้องมี **ฐานข้อมูลกลาง** เพิ่มเข้ามา ทางที่สมเหตุสมผลที่สุดคือฝากไว้กับเซิร์ฟเวอร์ของระบบคิวที่มีอยู่แล้ว
เพราะมีระบบผู้ใช้ สิทธิ์ และการบันทึกตาม PDPA อยู่แล้ว โดยเพิ่ม API ประมาณ
`GET/POST /api/portfolio/{residents,activities,rotations,sessions,cases}` แล้วให้หน้านี้ซิงก์ขึ้นลง
ส่วนที่เหลือของแอปไม่ต้องแก้ เพราะทุกอย่างอ่าน–เขียนผ่านชั้น `store` ชั้นเดียว

## เชื่อมกับระบบคิวห้องผ่าตัดเพื่อลง logbook

หน้า **“Logbook เคสผ่าตัด”** ดึงรายการเคสจากระบบคิวห้องผ่าตัดมาใช้เป็นสมุดบันทึกการผ่าตัดของแพทย์ประจำบ้าน
ต่อได้ 3 ทาง เลือกทางที่ตรงกับการติดตั้งจริง

1. **อ่านจากหน้าระบบคิวในที่เก็บเดียวกัน** — กดปุ่มเดียว หน้านี้จะอ่าน `../index.html` (หน้าสาธิตระบบคิว)
   ดึงข้อมูลเคสที่ฝังอยู่ในหน้านั้นออกมา ใช้ได้เมื่อเปิดผ่าน GitHub Pages หรือเว็บเซิร์ฟเวอร์
   (เปิดจาก `file://` เบราว์เซอร์จะไม่ยอมให้อ่านไฟล์ข้างเคียง)
2. **นำเข้าไฟล์** — JSON ที่ export จากระบบคิว หรือ CSV ที่มีหัวคอลัมน์
   `date, hn, diagnosis, operation, subspecialty, surgeon, assistant, room, anesthesia, asa, side, duration, id`
3. **ต่อ API ของระบบจริง** — ตั้งที่อยู่ระบบคิวในหน้าตั้งค่าการเชื่อมต่อ ระบบจะเรียก
   `GET {ที่อยู่}/api/cases?scope=all&includeClosed=true` พร้อม cookie ของผู้ใช้ (ต้องล็อกอินระบบคิวอยู่ และฝั่งระบบคิวต้องอนุญาต CORS)
   หน้านี้ไม่เก็บรหัสผ่านหรือโทเคนใด ๆ

เคสซ้ำจะถูกอัปเดตทับ ไม่สร้างรายการซ้ำ (จับคู่ด้วยเลขที่เคสของระบบคิว) และ **ชื่อผู้ป่วยไม่ถูกนำเข้ามาโดยตั้งใจ**
สิ่งที่นำเข้าคือ วันที่ HN อายุ เพศ การวินิจฉัย การผ่าตัด ข้าง อนุสาขา ห้อง ประเภทเคส การระงับความรู้สึก ASA
ระยะเวลา และชื่อศัลยแพทย์หลัก · HN แสดงแบบปิดบังไว้ก่อน กดเปิดดูเต็มได้เมื่อต้องตรวจสอบ

### ใครร่วมผ่าตัดเคสไหน

ข้อจำกัดที่ต้องรู้: **ระบบคิวรุ่นปัจจุบันบันทึกเฉพาะศัลยแพทย์หลัก ยังไม่มีช่องผู้ช่วยผ่าตัด**
(ในข้อมูลสาธิต `assistantSurgeonNames` ว่างทุกเคส) หน้านี้จึงเติมชั้น “ผู้ร่วมผ่าตัด” ให้เอง โดย

- จับคู่ชื่อที่มีอยู่ในเคส (ศัลยแพทย์หลัก ผู้ช่วย ผู้ลงข้อมูล) กับรายชื่อแพทย์ประจำบ้านให้อัตโนมัติ
- ถ้าจับคู่ไม่ได้ **เสนอชื่อจากตารางหมุนเวียน** — ใครหมุนเวียนอยู่หน่วยที่ตรงกับอนุสาขาของเคสในวันนั้น
  กดปุ่มเดียวเพื่อเติมทั้งหมด แล้วค่อยไล่แก้บทบาททีหลัง
- บทบาทที่บันทึกได้: ผู้ผ่าตัดหลัก / ผู้ช่วยที่ 1 / ผู้ช่วยที่ 2 / ผู้สังเกตการณ์ พร้อมช่องให้อาจารย์รับรอง

จำนวนเคสแยกตามบทบาทนับรวมเป็นเกณฑ์ความก้าวหน้าของแต่ละชั้นปี (ตั้งค่าได้ที่ `เกณฑ์ › เคสผ่าตัด`) แสดงในแฟ้มรายบุคคล
และพิมพ์เป็น logbook รายคนได้ (สรุปตามอนุสาขา + รายการเคสเรียงลำดับ + ช่องลงนามอาจารย์ผู้รับรอง)

**เกณฑ์เคสผ่าตัดสะสมตลอดหลักสูตร ไม่ใช่โควตาต่อปี** — ตามตารางภาคผนวกที่ 4 ของราชวิทยาลัยฯ
("ประสบการณ์การสะสมจำนวนหัตถการใน Electronic logbook") ค่าตั้งต้นในระบบ:

| ชั้นปี | ผู้สังเกตการณ์ขึ้นไป | ผู้ช่วยผ่าตัดขึ้นไป | ผู้ผ่าตัดหลัก |
|---|---|---|---|
| 1 | 30 | 0 | 0 |
| 2 | 30 | 30 | 10 |
| 3 | 30 | 60 | 30 |
| 4 | 30 | 100 | 50 |

และ**เคสขั้นสูงกว่านับแทนขั้นต่ำกว่าได้เสมอ** — เคสที่เป็นผู้ผ่าตัดหลักหนึ่งเคส เข้าเกณฑ์ทั้งสามแถวพร้อมกัน
(ผ่าตัดหลัก + ผู้ช่วยขึ้นไป + ผู้สังเกตการณ์ขึ้นไป) เคสที่เป็นผู้ช่วยเข้าเกณฑ์สองแถวหลัง `progressFor()` คำนวณแบบนี้ทั้งระบบ
(แฟ้มรายบุคคล แดชบอร์ด HTML/PPTX สิ่งที่ยังขาด) แก้ตัวเลขได้ที่หน้าตั้งค่า › เกณฑ์ เมื่อราชวิทยาลัยฯ ปรับเกณฑ์ใหม่

**ถ้าอยากให้อัตโนมัติเต็มรูปแบบ** ระบบคิวต้องเพิ่มการบันทึกผู้ร่วมผ่าตัดในเคส เช่นฟิลด์
`attendees: [{ userId, role }]` หรือเติมชื่อลง `assistantSurgeonNames` ให้ครบ เมื่อมีข้อมูลนั้นแล้ว
หน้านี้จะจับคู่ให้เองทันทีโดยไม่ต้องยืนยันทีละเคส

## กติกาการจัดหมุนเวียนที่ระบบใช้

ปีการศึกษาเริ่ม **1 กรกฎาคม** จบ **30 มิถุนายน** แบ่งเป็น 12 บล็อก บล็อกละ 1 เดือน

| ชั้นปี | การหมุนเวียน |
|---|---|
| ปี 1 | วนนอกภาควิชา 3 เดือน — วิสัญญีวิทยา, โรคข้อ (rheumatology), **ศัลยกรรมอุบัติเหตุ (Trauma Surgery)** อย่างละ 1 เดือน · ที่เหลือวนตามสาย |
| ปี 2 | เวชศาสตร์ฟื้นฟู (PM&R) 1 เดือน · ที่เหลือวนตามสาย |
| ปี 3 | วนตามสาย + free elective 3 เดือน (ไปพร้อมกันได้ไม่เกิน 2 คน) · เป็นหัวหน้าสาย — ถ้าเดือนนั้นสายไม่มีปี 3 ปี 4 ที่ลงสายทำแทน |
| ปี 4 | วนหน่วยอนุสาขา**ให้ครบทุกหน่วย**ก่อน · เดือนที่เหลือเป็นกำลังเสริมให้สายที่ขาด **สายละไม่เกิน 1 เดือน และไม่จำเป็นต้องลงสายจนครบ** · ตั้งแต่ มี.ค. เป็น free elective พร้อมกันทั้งชั้นปีถึง มิ.ย. |

> **trauma ของปี 1 คือ Trauma Surgery ของกลุ่มงานศัลยกรรมอุบัติเหตุ ไม่ใช่หน่วย Trauma / Paediatric ของออร์โธฯ**
> หน่วยอนุสาขาของออร์โธฯ ทั้งห้าหน่วยเป็นของปี 4 · เคยป้อนตารางจริงผิดตรงนี้มาแล้วและไม่มีอะไรทักท้วง

**กติกาชั้นปีของหน่วยเก็บไว้ที่ตัวหน่วยเอง** (`services[].years`) ที่เดียว — รายชื่อหน่วยที่แต่ละชั้นปี
ต้องไปวนนอกภาควิชาถูก*คำนวณ*จากค่านี้ ไม่ได้เขียนซ้ำอีกที่ และระบบ**ตรวจตารางที่บันทึกไว้จริง**
(ไม่ใช่เฉพาะแผนที่ระบบสร้าง) แล้วขึ้นแถบเตือนบนหน้าตารางเมื่อมีใครถูกลงหน่วยที่ไม่ใช่ของชั้นปีตัวเอง
พร้อมบอกชื่อคน ชั้นปี หน่วย และชั้นปีที่หน่วยนั้นอนุญาต

เงื่อนไขเพิ่มเติมที่ตัวจัดแผนพยายามรักษา

- การวนนอกภาควิชาของชั้นปีเดียวกัน **ไม่ให้ชนกัน** (ไม่เกิน ⌈จำนวนคนในชั้นปี ÷ 3⌉ คนต่อเดือน)
- การลงสาย **แบ่งจำนวนคนให้เท่ากันที่สุด** ในแต่ละเดือน และเลี่ยงการอยู่สายเดิมสองเดือนติด
- **คุมองค์ประกอบรุ่นในสาย** — เกลี่ยรุ่นพี่ (ปี 3–4) และรุ่นน้อง (ปี 1–2) แยกกัน ไม่ให้สายใดมีแต่รุ่นน้องหรือมีแต่รุ่นพี่
  ถ้าสายใดไม่มีรุ่นพี่ ระบบจะย้ายปี 3 จากสายที่มีรุ่นพี่เกินมาให้ก่อน
- **ปี 4 เป็นกำลังเสริม** — ระบบจะกันเดือนให้ครบทุกหน่วยอนุสาขาก่อนเสมอ เดือนที่เหลือจึงถูกจัดไปช่วยสายที่ว่าง
  หรือสายที่ไม่มีรุ่นพี่ โดย **สายละไม่เกิน 1 เดือนต่อคน** ถ้าไม่มีสายไหนขาดก็ไม่ต้องลงสาย (ได้อยู่หน่วยอนุสาขาต่อ)
  และในเดือนเดียวกันปี 4 จะไม่ซ้ำหน่วยอนุสาขากันเอง

หน้าตารางรายเดือนมีตาราง **“องค์ประกอบของแต่ละสาย”** อยู่ใต้ตารางหลัก แสดงจำนวนคนในสายแต่ละเดือนพร้อมสัดส่วน
ปี 3–4 / ปี 1–2 ทำสีให้เห็นทันทีว่าเดือนไหนสายว่าง ไม่มีรุ่นพี่ หรือรุ่นน้องกระจุก

ตรวจแล้วทุกขนาดรุ่น: ปี 4 ทุกคน**วนครบทั้ง 5 หน่วยอนุสาขา** และ**ไม่เคยลงสายเดิมซ้ำ**

ผลทดสอบกับขนาดรุ่นต่าง ๆ (48 ช่อง = 4 สาย × 12 เดือน)

| ขนาดรุ่น | สายว่าง | สายที่ไม่มีรุ่นพี่ | ขนาดสายเฉลี่ย | ปี 4 ได้ sub/สาย/elective |
|---|---|---|---|---|
| ชั้นปีละ 2 คน (8) | 0 | 24 | 1.3 | 5 / 3 / 4 เดือน |
| ชั้นปีละ 3 คน (12) | 0 | 13 | 2.0 | 5–6 / 2–3 / 4 |
| ชั้นปีละ 4 คน (16) | 0 | 3 | 2.6 | 5–6 / 2–3 / 4 |
| ชั้นปีละ 5 คน (20) | 0 | 0 | 3.1 | 6–7 / 1–2 / 4 |

ยิ่งรุ่นใหญ่ยิ่งลงตัว ส่วนรุ่นเล็กมากจะมีบางเดือนที่สายมีแต่รุ่นน้อง ซึ่งเป็นข้อจำกัดของจำนวนคนจริง ๆ
ระบบจึงรายงานออกมาให้ผู้จัดตารางตัดสินใจ แทนที่จะซ่อนไว้
- หน่วยอนุสาขาที่อยู่คู่กัน: **Trauma/Paediatrics · Hand/Tumour · Foot & Ankle/Sports** ส่วน Spine และ Arthroplasty แยกหน่วย
- ไม่มีการวน ER เป็นบล็อกแยก (งานห้องฉุกเฉินอยู่ในเวรของแต่ละสาย)

ปุ่ม “สร้างแผนอัตโนมัติ” สร้างแผนทั้งปีตามกติกานี้ แล้วรายงานสิ่งที่ยังไม่ลงตัว เช่น เดือนที่สายใดไม่มีคนลง
หรือเดือนที่ elective เต็มเพดาน เพื่อให้ผู้จัดตารางแก้เองต่อได้ (ทุกบล็อกยังกดแก้ทีละช่องได้ตามปกติ)

**ตารางจริงของกลุ่มงาน**ไม่จำเป็นต้องตรงกติกาข้างบนทุกข้อ (เช่น ปีการศึกษา 2569 ปี 3 บางคน elective ไม่ติดกัน 3 เดือน)
ระบบจึงฝังตารางจริงไว้ต่างหาก (`ROTATION_PRESETS` คีย์ = ปีการศึกษา) และลงตามนั้นทั้งปีผ่านปุ่ม “ใช้ตารางจริงของกลุ่มงาน”
โดยไม่ผ่านตัวจัดแผน — กติกาชั้นปีของหน่วยยังถูกตรวจและขึ้นเตือนเหมือนเดิม
ในปีที่มีตารางจริง ตัวจัดแผนจึงเป็นตัวช่วยเติมคนที่ตารางจริงยังไม่มี (เช่น แพทย์ประจำบ้านที่เพิ่งเพิ่มเข้าระบบ) ไม่ใช่ตัวเขียนทับทั้งปี

## ชื่ออนุสาขาเป็นภาษาอังกฤษทั้งหมด

อนุสาขาที่ระบบเก็บและแสดงมี 8 อนุสาขา ใช้ชื่อภาษาอังกฤษอย่างเดียว ทั้งในตัวเลือก ตารางความครอบคลุม รายงาน
และไฟล์ CSV ชื่อสั้นที่สุดเท่าที่ยังไม่กำกวม — ไม่มีคำว่า Orthopaedic หรือ Musculoskeletal ห้อยท้าย
เพราะทั้งหน้าเป็นออร์โธปิดิกส์อยู่แล้ว และหัวคอลัมน์ในตารางความครอบคลุมต้องอ่านออกตอนตะแคง

| id | ชื่อที่แสดง |
|---|---|
| `trauma` | Trauma (รวมการติดเชื้อกระดูกและข้อ) |
| `spine` | Spine |
| `arthroplasty` | Arthroplasty |
| `sports` | Sports |
| `hand` | Hand |
| `foot` | Foot & Ankle |
| `pediatric` | Paediatric |
| `oncology` | Tumour |
| `general` | General / Unclassified (ไม่ถูกนับในตารางความครอบคลุม) |

`id` ไม่เปลี่ยนตามชื่อที่แสดง ข้อมูลที่บันทึกไว้แล้วจึงไม่กระทบ (`oncology` ยังเป็น `oncology` แม้จะแสดงว่า Tumour)

หมวดที่เคยมีแล้วถูกยุบเข้าหมวดที่รับช่วงต่อ พร้อมคำสำคัญของหมวดเดิม:
**Musculoskeletal Infection → Trauma** (osteomyelitis, septic arthritis, PJI, necrotizing fasciitis,
วัณโรคกระดูก) · Shoulder & Elbow → Sports · Metabolic Bone Disease → Trauma · Basic Science → General
ข้อมูลเก่าที่บันทึกไว้ในเบราว์เซอร์ถูกย้ายให้อัตโนมัติเมื่อเปิดหน้าเว็บครั้งถัดไป ทั้งช่องอนุสาขาเดี่ยว
(กิจกรรม เคสผ่าตัด งานวิจัย ตารางนำเสนอ) และช่องที่เป็นอาเรย์ (อนุสาขาของอาจารย์ ของหน่วย และของคาบในตารางประจำสัปดาห์)
รหัสที่ระบบคิวส่งมาว่า `infection` ก็ถูกรับเข้าเป็น `trauma` ด้วย — มีข้อทดสอบยืนยันทุกข้อ

ส่วนคำสำคัญที่ใช้เดาอนุสาขายังมีทั้งคำไทยและคำอังกฤษ เพื่อให้จับสไลด์ที่เขียนด้วยภาษาไทยได้

## การผูกกับมาตรฐาน WFME

ทุกกิจกรรมถูกผูกกับหัวข้อมาตรฐานไว้ตั้งแต่ตอนบันทึก เช่น topic presentation ผูกกับ 2.1, 2.3, 3.1, 6.5 และ
trauma film conference ผูกกับ 2.3, 2.6, 6.2 หน้า “มาตรฐาน WFME” จึงแสดงได้ว่าแต่ละหัวข้อมีหลักฐานอะไรอยู่จริง
กี่รายการ และหัวข้อใดยังไม่มี

logbook เคสผ่าตัดที่ดึงจากระบบคิว ตารางหมุนเวียนรายเดือน ตารางเซสชันประจำสัปดาห์ ตารางนำเสนอ และทะเบียนอาจารย์
ถูกนับเป็นหลักฐานของหมวด
2.4 / 2.5 / 2.6 (โครงสร้างหลักสูตรและความสัมพันธ์กับงานบริการ), 3.1 / 3.2 (วิธีการประเมินและความเชื่อมโยงกับการเรียนรู้),
5.1 / 5.2 (อาจารย์และหน้าที่ของอาจารย์), 6.2 (สถานที่ฝึกปฏิบัติทางคลินิก), 6.4 (ทีมผู้ให้บริการทางคลินิก)
และ 8.4 (การบริหารจัดการและงานธุรการ)

สิ่งที่กิจกรรมการนำเสนอตอบไม่ได้ (พันธกิจ ผลลัพธ์การเรียนรู้ เอกสารหลักสูตร) บันทึกแยกไว้ในหน้า
“ตั้งค่า & ข้อมูล” เป็นข้อความพันธกิจ รายการผลลัพธ์การเรียนรู้ และทะเบียนเอกสาร ที่ผูกเข้ากับหัวข้อมาตรฐานได้เอง

> **ข้อควรระวัง** หมวดและหัวข้อในหน้านี้เรียบเรียงตามโครง 9 หมวดของ *WFME Global Standards for Quality
> Improvement: Postgraduate Medical Education* (ฉบับปรับปรุง ค.ศ. 2015) เพื่อใช้จัดหมวดหลักฐานภายในกลุ่มงาน
> **ยังไม่ได้ทาบถ้อยคำและหมายเลขกับเอกสารต้นฉบับทีละข้อ** ก่อนนำไปใช้ยื่นประเมินจริง ผู้รับผิดชอบหลักสูตร
> ควรตรวจสอบกับเอกสารทางการของ WFME และเทียบกับเกณฑ์ของแพทยสภา/ราชวิทยาลัยแพทย์ออร์โธปิดิกส์
> แห่งประเทศไทยอีกชั้นหนึ่ง แก้ไขข้อความได้ที่ตัวแปร `WFME_AREAS` ในไฟล์ `index.html`

เกณฑ์จำนวนกิจกรรมต่อชั้นปีที่ใส่มาให้เป็น **ค่าตั้งต้นสำหรับสาธิต ไม่ใช่เกณฑ์ทางการ** แก้ได้ในหน้าตั้งค่า

## ตารางประจำสัปดาห์: ส่วนกลาง + หน่วย + สาย

ตารางของแต่ละคนไม่ได้เขียนทีละคน แต่เกิดจากสามชั้นซ้อนกัน แล้วระบบคูณกับ**ตารางหมุนเวียน**ให้เอง

| ชั้น | ใครได้ | ตัวอย่าง |
|---|---|---|
| **ส่วนกลางของภาควิชา** | ทุกคน ไม่ว่าเดือนนั้นอยู่หน่วยไหน | morning report, grand round, journal club |
| **หน่วยที่วนอยู่** | คนที่อยู่หน่วยนั้นในเดือนนั้น | OR ของหน่วย trauma, OPD ของสายฟ้า |
| **หน่วยอนุสาขา** | คนที่วน sub — **หน่วยอนุสาขาไม่มีตารางของตัวเอง งานแบ่งไปอยู่ในสาย** คนวน sub จึงตามลิสต์ของอนุสาขาตัวเองในทุกสาย | R4 วน hand/tumour ตาม OR hand ของสายฟ้า/แดง/เหลือง และ OR tumour ของสายแดง/เหลือง |
| **ข้อมูลเวร/วันหยุดที่เคยลงไว้** | คนที่ข้อมูล `duty` ของวันนั้นระบุไว้ (ไม่มีหน้ากรอกใหม่แล้ว) | อยู่เวร ER, วันหยุดราชการ |

- **เขียนครั้งเดียวใช้ทั้งปี** — 10 หน่วย × ~5 คาบ ≈ 50 บรรทัด แทนที่จะเขียน 8 คน × 5 คาบ × 48 สัปดาห์ ≈ 2,000 บรรทัด
  และเมื่อมีคนสลับสาย ไม่ต้องแตะตารางเลย เพราะตารางหมุนเวียนจัดการให้
- **หนึ่งคาบมีอาจารย์ได้หลายคน** เพราะสายหนึ่งมีอาจารย์จากหลายอนุสาขาปนกัน
  เปิดรายละเอียดเซสชันจึงเลือกได้ว่าใครเป็นผู้รับผิดชอบจริงในวันนั้น
- **คาบส่วนกลางจำกัดชั้นปีได้** (เช่น inter-hospital conference เฉพาะปี 4) และเลือกได้ว่าคนที่วนนอกภาควิชาต้องเข้าด้วยหรือไม่
- **คาบของอาจารย์ล้วน** ติดธง `staffOnly` ไว้ได้ — อยู่ในตารางเป็นบันทึก แต่ไม่สร้างคาบให้แพทย์ประจำบ้าน
  (คลินิกพิเศษออร์โธเด็กบ่ายพฤหัสบดีไม่มีแพทย์ประจำบ้านเข้า จึงไม่ได้อยู่ในตารางนี้เลย
  ดูได้ที่ทะเบียนอาจารย์แทน)

### เมื่อคาบชนกัน

- คาบที่ **ไม่ระบุเวลา** = ใช้ทั้งช่วง (OR ทั้งเช้า) · คาบที่ **ระบุเวลา** = แทรกอยู่ในช่วงนั้น (ประชุม 07:30–08:30)
- ถือว่าชนกันเมื่อ **ทั้งคู่ใช้ทั้งช่วงเดียวกัน** หรือ **เวลาที่ระบุคาบเกี่ยวกัน** —
  ถ้าอันหนึ่งระบุเวลาอีกอันไม่ระบุ ไม่นับว่าชน ไม่งั้นประชุมเช้าจะขึ้นว่าชนกับ ward round ทุกวันซึ่งไม่จริง
- ตัดสินด้วย **ลำดับความสำคัญ** ตั้งได้ที่หน้าตั้งค่า ค่าเริ่มต้น
  **กิจกรรมวิชาการ 40** · ส่วนกลาง 30 · ตารางเวรรายเดือน 25 · หน่วยอนุสาขา 20 · สาย 10
  และแต่ละคาบตั้งตัวเลขเฉพาะของตัวเองทับได้
- **กิจกรรมวิชาการเป็นหน้าที่แรกของแพทย์ประจำบ้าน มาก่อนงานบริการเสมอ** — คาบที่ติดธง `academic`
  ได้ลำดับสูงสุด และการบันทึกรายวันก็ชนะได้แค่คาบงานบริการด้วยกัน ไม่ชนะกิจกรรมวิชาการ
  (เช่น ปี 4 ต้องเข้า inter-hospital conference บ่ายพฤหัสบดีเสมอ)
- คาบที่แพ้ **ยังแสดงอยู่ในตาราง** (การ์ด "ตอนนี้ใครอยู่หน่วยไหน" และหน้า "วันนี้") แบบเส้นประและจาง
  พร้อมบอกว่าไปชนกับอะไร — ไม่ซ่อน เพราะคนจัดตารางต้องเห็น
- **ถ้าลำดับเท่ากัน ระบบจะไม่ตัดสินแทน** แต่ทำเครื่องหมายว่าชนกันเพื่อให้คนเลือกเอง

### ตารางเวรรายเดือน — ถอดหน้ากรอกออกแล้ว

OPD และ ER วันจันทร์–ศุกร์วนตามสายตามตารางประจำสัปดาห์ของแต่ละสายอยู่แล้ว ส่วนวันพฤหัสบดี (อาจารย์ออก OPD 1–2 คน และสาย ER
ที่ไม่ซ้ำกับ OPD วนไปเรื่อย) กลุ่มงานจัดเองทุกเดือน จึงไม่ต้องมีตารางให้ลงในระบบ กติกาเดิมคงอยู่: **อาจารย์ที่รับผิดชอบ ER
เป็นผู้รับเคสใหม่และดูแล OR emergency** · ข้อมูล `duty` ที่เคยลงไว้ (เวร ER, วันหยุดราชการ) ยังถูกอ่านใน `computeSessions()`
และปฏิทิน (วันหยุดไม่มีช่อง F/P) ตามเดิม แค่ไม่มีหน้ากรอกใหม่ · `weeklyDutyPattern()` ยังบอกได้ว่าวันไหนสายไหน OPD/ER

### เลือกเป็นรายวันว่าเข้าคาบไหน กับอาจารย์คนใด

ลำดับความสำคัญเป็นกติกาของทั้งปี แต่ของจริงบางวันไม่เป็นไปตามนั้น — โดยเฉพาะคนที่วนตามสาย
ซึ่งมักไม่ได้ชนกันเป็นระบบ แค่ต้องเลือกว่าวันนี้ไปห้องไหน กับอาจารย์คนไหน

- เปิดคาบใดก็ได้จากการ์ด "ตอนนี้ใครอยู่หน่วยไหน" หรือหน้า "วันนี้" แล้วกด **“บันทึกว่าเข้าคาบนี้”**
  พร้อมเลือกอาจารย์ผู้คุมของวันนั้น
- **การเลือกรายวันชนะลำดับความสำคัญของงานบริการ** และมีผลเฉพาะวันนั้น ไม่กระทบสัปดาห์อื่น — แต่ไม่ชนะกิจกรรมวิชาการ
- คาบอื่นในช่วงเดียวกันของวันนั้นจะขึ้นว่า “วันนี้เลือกเข้า …” แยกจากกรณีที่แพ้เพราะลำดับความสำคัญ
- **เจ้าของตารางบันทึกเองได้** ไม่ต้องรออาจารย์ แต่บันทึกแทนคนอื่นไม่ได้ (อาจารย์และผู้จัดหลักสูตรทำแทนได้)
- อาจารย์ที่เลือกไว้เป็นแค่บันทึกว่าวันนั้นอยู่กับใคร (`sessionPicks`) ไม่ผูกกับการประเมินใด ๆ อีกต่อไป

## กติกาการโต้ตอบที่ใช้ทั้งแอป

สิ่งที่ผู้ใช้คาดหวังได้เหมือนกันทุกหน้า (ทุกข้อมีเทสต์คุมอยู่ใน `tests/dialogs`, `tests/feedback`):

- **กล่องโต้ตอบ** — เปิดแล้ว focus อยู่ที่ช่องกรอกแรก · กด Enter ในช่องกรอก = กดปุ่มหลัก (บันทึก) ไม่ใช่ปิดกล่องทิ้งข้อมูล
  · Esc = ปิดโดยไม่บันทึก · กล่องที่ไม่มีปุ่มหลัก (ดูอย่างเดียว) กด Enter แล้วไม่มีอะไรเกิดขึ้น
- **error อยู่ที่ช่อง** — ช่องที่กรอกไม่ครบถูกทำเครื่องหมายสีแดงพร้อมข้อความใต้ช่อง focus ย้ายไปที่ช่องแรกที่ผิด
  และหายเองเมื่อพิมพ์แก้ · แบบประเมินลงกองทำเครื่องหมายทุกข้อบังคับที่ยังไม่ตอบ ไม่ใช่แค่ 3 ข้อแรก
- **การลบเลิกทำได้** — ลบรายการเดี่ยว (กิจกรรม เคส ช่วงหมุนเวียน อาจารย์ หน่วย ฯลฯ) หายทันทีโดยไม่ถามก่อน
  แต่มีปุ่ม **เลิกทำ** ใน toast 6 วินาที กดแล้วกลับมาที่เดิม รวมของที่ถูกลบตามไปด้วย (เช่นลบหน่วยแล้วช่วงหมุนเวียนของหน่วยนั้น)
  · การกระทำที่กระทบทั้งฐานข้อมูล (ลบทั้งหมด โหลดสาธิต นำเข้าไฟล์ ดึงจากคลาวด์ สร้างแผนทั้งปี) ยังถามยืนยันก่อน
  ด้วยกล่องของแอป — ไม่มี popup `confirm()` ของเบราว์เซอร์เหลืออยู่ที่ไหนเลย
- **ปุ่ม Back ของเบราว์เซอร์** พากลับไปหน้าก่อนหน้าจริง และลิงก์ที่มี `#ชื่อหน้า` เปิดหน้านั้นได้โดยตรง
- **หน้าไม่ยาวเกิน — การ์ดพับได้และรายการค่อย ๆ แสดงเพิ่ม** — ส่วนที่ไม่ใช่งานหลักของหน้าพับไว้เป็นหัวข้อบรรทัดเดียว
  พร้อมป้ายจำนวน (เช่น "44 รายการ") กดหัวข้อ (หรือ Enter) เพื่อขยาย/พับ ระบบจำสถานะไว้ในเครื่องต่อการ์ด (`localStorage`
  ไม่ซิงก์ ไม่ติดไปกับไฟล์สำรอง) · ซ่อนด้วย CSS เท่านั้น — ปุ่ม ช่องกรอก และตารางข้างในยังอยู่ครบ ดัชนีของหน้าตั้งค่าและ
  ปุ่ม "ไปทำ" ของ checklist เปิดการ์ดเป้าหมายให้เอง · **สั่งพิมพ์เห็นครบทุกส่วนเสมอ** ไม่ว่าจะพับอะไรไว้
  · ที่พับไว้ก่อน: หน้าตั้งค่าทุกการ์ดยกเว้น checklist เตรียมปี · หน้าตาราง: ตารางเวรรายเดือน
  ผู้นิเทศ Topic conference การแลกวัน การขอไปเข้าคาบสายอื่น · หน้ามาตรฐาน WFME ทั้ง 9 หมวด
  (หัวข้อบอก จำนวนหลักฐาน และ กี่ข้อมีกิจกรรม/ยังไม่มีหลักฐาน) · หน้าแพทย์ประจำบ้าน: งานวิจัย การหมุนเวียน/ลงกอง
  logbook (ความก้าวหน้า ส่วนที่ขาด และบันทึกกิจกรรมยังเปิดอยู่) · ภาพรวม EPA ทั้งกลุ่มงาน
  · การ์ด "ตอนนี้ใครอยู่หน่วยไหน" พับไว้ทั้งใบ (ป้ายบอกจำนวนคน) และข้างในยุบคาบของแต่ละคนเป็นบรรทัด "วันนี้ n เซสชัน · คาบแรก" กดดูได้
  · หน้ารายชื่อแพทย์ประจำบ้านเป็นภาพรวมอย่างเดียว แฟ้มรายบุคคลเป็นหน้าย่อยของตัวเอง
  · คำอธิบายยาว ๆ ใต้หัวข้อการ์ดพับเป็นบรรทัด ⓘ · รายการยาว (เคสทั้งหมดในหน้า logbook แสดง 30 แถวแรก)
  มีปุ่ม "แสดงเพิ่มอีก" และ "แสดงทั้งหมด" แทนการตัดทิ้งเงียบ ๆ ที่ 300 แบบเดิม · ไฟล์เดียวควบคุมทั้งหมด: ใส่
  `data-fold="รหัส"` (และ `data-fold-closed` ถ้าจะพับไว้ก่อน) ให้กล่องที่ลูกคนแรกเป็นหัวข้อ ที่เหลือระบบทำให้
- **ภาพรวมก่อน แล้วค่อยเลือกคน** — อาจารย์และผู้จัดหลักสูตรเปิดทุกหน้ามาเจอภาพรวมทั้งกลุ่มก่อน แล้วค่อยเลือกดูรายคน:
  รายชื่อแพทย์ประจำบ้าน → "เปิดแฟ้ม" · EPA ภาพรวมทั้งกลุ่มงานมาก่อน การ์ดรายบุคคลพับไว้จนกว่าจะเลือกคน
  · ปฏิทินเป็นตารางรวมของทั้งภาควิชา ไม่มีขั้นเลือกคน (แพทย์ประจำบ้านสลับ “ของฉัน” ได้ อาจารย์เห็นรวมอย่างเดียว)
  · Logbook สรุปรายบุคคลก่อนเคสทั้งหมด · หน้าประเมินและความครอบคลุมเป็นภาพรวมอยู่แล้ว
  **แพทย์ประจำบ้านกลับกัน** — เห็นของตัวเองทันที: กลุ่ม "แพทย์ประจำบ้าน" เปิดแฟ้มของตัวเองเลย (ไม่มีแท็บรายชื่อ)
  EPA และปฏิทินเป็นของตัวเอง หน้าวันนี้เป็นคาบของตัวเอง
- **มือถือ** — เลื่อนลงแล้วแถบบนย่อเหลือแท็บ (จาก ~212px เหลือ ~106px) · ตารางที่กว้างกว่าจอมีเงาและข้อความบอกว่าเลื่อนได้
  · แถวตัวกรองเรียงสองคอลัมน์
- **หน้าตั้งค่า** — เรียงจากที่ใช้บ่อย (บัญชี สำรองข้อมูล) ไปหาที่ตั้งครั้งเดียว มีดัชนีกระโดดบนสุด
  · การ์ด "เตรียมปีการศึกษา" เป็น checklist 8 ข้อที่คำนวณสดจากข้อมูล (อาจารย์ หน่วย แพทย์ประจำบ้าน บัญชี แผนหมุนเวียน
  ตารางเวร ผู้นิเทศ Topic conference เกณฑ์) กดไปทำได้จากตรงนั้น · ปุ่มลบทั้งหมด/โหลดสาธิตอยู่ใน "โซนอันตราย" ท้ายสุด
- **เพิ่มแพทย์ประจำบ้าน = ได้บัญชีทันที** — กล่องเพิ่มคนมีช่องชื่อผู้ใช้/รหัสผ่านให้เลย (ปิดได้) รายชื่อคนที่ยังไม่มีบัญชีขึ้นป้าย
  พร้อมปุ่มสร้างบัญชี · บัญชี resident ที่ไม่ได้ผูกกับใครจะเห็นข้อความบอกสาเหตุ ไม่ใช่หน้าว่าง
- **วันที่** — ปี พ.ศ. 4 หลักทุกที่ (`2 ก.ย. 2569`, `ก.ย. 2569`, `2 ก.ย. 2569 14:30`) และช่องวันที่ของเบราว์เซอร์มีคำอ่านแบบไทยใต้ช่อง

## ชุดทดสอบและการตรวจอัตโนมัติ

หน้านี้เป็นไฟล์เดียวที่ไม่มีขั้นตอน build จึงไม่มีอะไรมาเตือนตอนพิมพ์ผิด — เคยพังมาแล้วจากการมี
`</script>` หลุดเข้าไปในโค้ด ซึ่งปิดบล็อกสคริปต์กลางคันและทำให้ทั้งแอปตาย ชุดทดสอบจึงมีไว้จับเรื่องนี้
และเรื่องที่สำคัญกว่า คือกันไม่ให้การแก้ครั้งต่อ ๆ ไปเผลอเปิดช่องสิทธิ์ที่ปิดไปแล้วกลับมาอีก

```
cd portfolio/tests
npm ci                                  # ครั้งแรกครั้งเดียว
npx playwright install chromium         # ครั้งแรกครั้งเดียว
node run.mjs                            # รันทุกชุด
node run.mjs permissions                # รันเฉพาะชุดสิทธิ์
```

| ชุด | ตรวจอะไร |
|---|---|
| `parse` | JavaScript ทั้งบล็อกคอมไพล์ผ่าน · ไม่มี `</script>` ที่ไม่ได้ escape · ไม่มี asset จากภายนอก · `sw.js` และ manifest ใช้ได้ |
| `smoke` | ล็อกอินได้ทั้งสามบทบาท · เดินทุกแท็บที่บทบาทนั้นเห็น (กลุ่มแพทย์ประจำบ้าน 5 หน้าย่อย) · จัดแผนหมุนเวียนได้ · หน้าจอมือถือ 390px ไม่ปัดข้างไปเจอที่ว่าง · หน้าความครอบคลุม (ภาพรวมและหน้าของแต่ละประเภท) ตรงกับข้อมูลจริงและตรงกับไฟล์ที่ส่งออก · ไม่มี error หลุดในคอนโซล |
| `permissions` | resident เข้ากลุ่มแพทย์ประจำบ้านแล้วเจอแฟ้มตัวเอง EPA ของตัวเองก่อน ปฏิทินรวมเห็นของคนอื่นได้แต่ "ของฉัน" กรองถูก · ทุกข้อในตาราง "เส้นแบ่งสิทธิ์ที่บังคับไว้จริงในโค้ด" ข้างต้น · การล้างข้อมูลผู้ป่วยออกจากข้อความสไลด์ · แดชบอร์ดทั้งกลุ่มงานและแดชบอร์ดรายบุคคลไม่หลุดชื่อคนอื่น |
| `schedule` | หน้าประเมินทั้งสองหน้าย่อย และการบันทึกผลประเมินลงกอง · ปุ่มคะแนนแตะครั้งเดียว เก็บค่าทศนิยมเดิมเมื่อไม่แตะ · รับรองฟอร์มว่างถูกปฏิเสธ · ผลโดยรวมที่แนะนำและการแก้ทับ · บันทึกแล้วเปิดคนถัดไป · การ์ดงานค้างลงกองของอาจารย์และตัวเลขค้างต่อเดือน · หน้า WFME อ้างทั้งสองแบบประเมินครบทุกหมวดที่ข้อระบุ · ตารางส่วนกลางถึงทุกคน · คาบเฉพาะชั้นปี · การชนกันของคาบและการตัดสินด้วยลำดับความสำคัญ · ลำดับเท่ากันแล้วไม่ตัดสินแทน · การเลือกรายวันชนะลำดับความสำคัญและมีผลเฉพาะวันนั้น · ตารางเวรรายเดือน เติมจากรูปแบบประจำสัปดาห์ อยู่เวร ER และวันหยุดราชการ · · ตารางจริงของกลุ่มงานปี 2569: ข้อมูลสาธิตตรงทุกช่อง ปุ่ม "ใช้ตารางจริงของกลุ่มงาน" กู้ตารางกลับหลังสร้างแผนอัตโนมัติได้ กันเลือกคนซ้ำ resident/staff ไม่เห็นปุ่ม
"สร้างแผนอัตโนมัติ" กันสำเนาช่วงหมุนเวียนเดิมไว้ก่อนเขียนทับ เป็น deep copy จริง รอดจากรีเฟรชหน้า กู้คืนจากประวัติได้และกู้คืนซ้ำได้อีก
เก็บสูงสุด 10 ชุดล่าสุดต่อปี · staff ไม่เห็นปุ่มประวัติแผน |
| `orqueue` | นำเข้าเคสจาก API ของระบบคิวผ่าน [`or-queue-mock/`](../or-queue-mock/) · กันนำเข้าซ้ำ · ระดับข้อมูลผู้ป่วยสามระดับ · การรับมือเมื่อระบบคิวมีปัญหา |
| `sync` | การรวมข้อมูลเมื่อสองเครื่องแก้ชนกัน — ทุกคอลเลกชันที่ซิงก์ต้องถูกรวมรายรายการ · ของที่เครื่องนี้เพิ่งบันทึกต้องไม่ถูกทับหาย · ล้างข้อมูลแล้วยังได้ก้อนข้อมูลที่ครบรูป |
| `topicconf` | ธง Topic conference ติดเฉพาะคาบนั้น · migration ของข้อมูลเก่า · ผู้นิเทศ/หัวหน้าสายคำนวณสด ไม่เดา · ตารางกำหนดผู้นิเทศแก้ได้เฉพาะผู้จัดหลักสูตร | · migration thursdaySub→thursdayTheme · ธีมวนตามรอบและกำหนดทับรายสัปดาห์
| `calendar` | ปฏิทินรวมการนำเสนอ: วันราชการข้างหน้ามีช่องประจำ F แล้ว P เรียงตามเวลา · วันหยุดราชการไม่มีช่อง · วันที่มีสไลด์ F ไม่มีช่อง F ซ้ำ · พฤหัสฯ ไม่มี F/P · staff/admin ไม่มีปุ่มสลับ เห็นหลายคน · chip = ตัวย่อ T/P/F/J + ชื่อเต็ม + หัวข้อย่อ ไม่มีป้าย/ผู้นิเทศ · ช่องประจำเส้นประ กดแล้วมีปุ่มไปหน้ารับสไลด์ (admin ลงตารางล่วงหน้าได้) · กด T กล่องมีชื่อเต็ม หัวข้อ ผู้นิเทศ ปุ่มแก้ไขเฉพาะ admin · resident เปิดมาเห็นทั้งกลุ่มงาน กด "ของฉัน" เหลือแต่ของตัวเอง เปิดกล่องของคนอื่นได้แต่ไม่มีปุ่มเปิดกิจกรรม · ผู้นิเทศที่กำหนดไว้โผล่ในกล่องของวันนั้น | · เช้าวันพฤหัสฯ 4 ช่วง ส.ค. 2569 ตรงตารางจริง (ธีม 4 สัปดาห์ · C/L · T คู่ผู้นำเสนอ/chief · K แทน J · X ทุกวันในช่วง) · chief ได้กิจกรรมกำกับอัตโนมัติ · ผู้จัด Kahoot ทั้งคู่ได้กิจกรรม · กล่องจัดการวันพฤหัสฯ · กล่องการประชุมภายนอก · "ของฉัน" รวม chief และ X ตามชั้นปี · ตารางจริง ก.ค.–ต.ค. 2569 (คาบแรกเป็นหัวข้อ resident → T สามช่อง · D Pediatric inter-hospital · สัปดาห์ที่ไม่มีหัวข้อ · quiz สามผู้จัด · KOKU/RCOST/workshop/ประชุมร่วมแทนทั้งวัน · วันหยุดเฉพาะวัน) · ลงชื่อเข้าร่วมได้กิจกรรม attend ที่ไม่มีแบบประเมินและไม่เข้าคิว
| `dialogs` | Enter ในกล่อง = บันทึก · Esc = ปิดไม่บันทึก · focus ช่องแรก · ปุ่ม Back/Forward ของเบราว์เซอร์เปลี่ยนหน้าจริง · hash ของหน้าที่ไม่มีสิทธิ์ถูกดึงกลับ |
| `feedback` | error อยู่ที่ช่อง · อาจารย์เห็นภาพรวม EPA ก่อน รายบุคคลพับจนกว่าจะเลือก ปฏิทินเปิดมาเห็นกริดรวมทันทีไม่มีขั้นเลือกคน · แบบประเมินลงกองทำเครื่องหมายทุกข้อที่ขาด · รายชื่อกับแฟ้มรายบุคคลคนละหน้า ตัวเลือกคน ปุ่มกลับ · อาจารย์หลายอนุสาขาเห็นและแก้ได้ · migrate ข้อมูลสาธิตเก่า (คำนำหน้า พญ. และ Trauma) โดยไม่แตะชื่อที่แก้เอง · การ์ดพับได้: พับไว้ก่อน ปุ่มข้างในยังอยู่ จำสถานะ รอด render ใหม่ ดัชนีเปิดให้ คีย์บอร์ด พิมพ์เห็นครบ · "แสดงเพิ่ม" ของช่วงหมุนเวียนและเคส · ลบแล้วเลิกทำได้ (รวมของที่ลบตามและผลประเมินลงกอง) · ผู้จัดหลักสูตรเห็นตัวเลขลงกองค้าง แพทย์ประจำบ้านเห็นผลลงกองรายข้อเป็นคำ · ไม่มี `window.confirm` เหลือ · หน้าตั้งค่ามีดัชนี h1 เดียว checklist 8 ข้อ · เพิ่มคนแล้วได้บัญชีที่ล็อกอินเห็นแค่ตัวเอง · วันที่ พ.ศ. 4 หลัก |

ทุกข้อในชุด `permissions` เคยเป็นช่องโหว่จริงมาแล้ว — ตรวจแล้วว่าถ้าเอาการกันสิทธิ์ออก ชุดทดสอบแดงจริง
อย่าลบข้อใดออกโดยไม่เข้าใจว่ามันกันอะไรอยู่

GitHub Actions (`.github/workflows/portfolio.yml`) รันทั้งสามชุดนี้ให้อัตโนมัติทุกครั้งที่มี push
หรือเปิด pull request ผลขึ้นเป็น ✅/❌ บน PR ก่อนที่ใครจะ merge

## ข้อจำกัดที่ต้องรู้ก่อนใช้

- **ข้อมูลอยู่ในเบราว์เซอร์ของเครื่องที่เปิดเท่านั้น** (localStorage) ไม่มีเซิร์ฟเวอร์ ไม่มีการรับส่งข้อมูลออกนอกเครื่อง
  เปิดคนละเครื่องจะไม่เห็นข้อมูลของกัน ใช้ปุ่มสำรอง/นำเข้าไฟล์ JSON เพื่อย้ายข้อมูล
- **ไม่ได้เก็บตัวไฟล์สไลด์** เก็บเฉพาะข้อความที่อ่านได้ ชื่อไฟล์ และค่าแฮช ต้นฉบับต้องเก็บไว้ที่อื่นตามระบบของกลุ่มงาน
- `.pptx` อ่านได้ครบที่สุด ส่วน `.pdf` อ่าน metadata ได้เสมอ แต่เนื้อความในสไลด์อ่านได้บ้างไม่ได้บ้าง
  ขึ้นกับวิธีฝังฟอนต์ (ภาษาไทยใน PDF มักอ่านไม่ออก) หน้าเว็บจะบอกเมื่ออ่านไม่ได้และให้กรอกเอง
- การเดาประเภทและอนุสาขาเป็นการช่วยกรอก ไม่ใช่คำตัดสิน ผู้บันทึกต้องตรวจก่อนยืนยันเสมอ
- ข้อมูลตัวอย่างไม่มีข้อมูลผู้ป่วยจริงเลย และชื่อแพทย์ประจำบ้าน/ผู้ป่วยในข้อมูลตัวอย่างเป็นชื่อสมมติทั้งหมด
  ส่วนชื่ออาจารย์/ผู้ประเมิน/ที่ปรึกษา ตั้งใจดึงจากทะเบียนอาจารย์จริงของกลุ่มงาน (ไม่ใช่ชื่อสมมติ)
  เพราะหน้าสาธิตถูกเอาไปให้อาจารย์ดูจริง

---

## For readers of this repository

A **demonstration page only**: one self-contained HTML file, no backend, no build step, no external
assets, no network requests. It tracks orthopaedic residents' training activities — topic presentations,
pre-/post-operative conferences, trauma film conferences, journal clubs and research progress.

The point of it is that recording happens *when the resident presents*: drop the `.pptx`/`.pdf` used for
the presentation onto the page and it is parsed in the browser (zip + OOXML for `.pptx`, object/stream
parsing for `.pdf`) to recover the title, author, creation date, slide count and slide text. Keyword
scoring over that text proposes the activity type and the orthopaedic subspecialty; the person recording
checks and confirms. The file's SHA-256 is kept as an evidence fingerprint and for duplicate detection —
the file itself is never uploaded or stored.

Every activity is tagged with the WFME postgraduate-standard sub-areas it can serve as evidence for, so
the programme can print an evidence report per standard area and per resident. The standards skeleton
follows the 9 areas of the WFME Global Standards for Quality Improvement (PGME, 2015 revision) but has
**not been reconciled word-for-word with the official document** — verify before using it for real
accreditation. All demo data is fictitious.

A second half of the page answers *where is each resident right now*, in three tables. A **monthly grid**
(residents x the 12 blocks of the academic year, July through June) colours each cell by what the resident
rotates through — the four ward teams named by colour, a paired subspecialty unit (trauma/paediatrics,
hand/tumour, foot & ankle/sports, spine, arthroplasty), an out-of-department month, or free elective. A
one-click planner lays out a whole year under the programme's rules (first-years take anaesthesia,
rheumatology and trauma surgery for a month each; second-years take PM&R; third-years take three elective
months, at most two of them away at once, and act as team chief when no fourth-year is on that team;
fourth-years rotate subspecialties until February and then take free elective together through June), keeps
out-of-department months from colliding within a year group, balances team numbers, and reports whatever it
could not satisfy. Colours, abbreviations and unit types are editable. A **weekly roster** expands the current
rotation into individual sessions — OR lists, OPD clinics, ward rounds, conferences, on-call — generated
automatically from each service's weekly template, each carrying the staff member responsible for it. A
**presentation schedule** records who presents what, on which day, at what time, in which room, and which
attending moderates; recording a slide deck auto-links to its scheduled entry, so entries with no evidence
recorded stand out.

The staff member responsible for a session evaluates the resident at the end of it: three domain scores, an
entrustment level (1-5) and written feedback, with a per-staff board of sessions still awaiting evaluation
over the last 14 days. Those evaluations count toward each year's requirements and serve as workplace-based
assessment evidence for WFME areas 3, 5 and 7; the rotation schedule evidences 2.4, 2.5, 2.6 and 6.2, and the
presentation schedule 2.5 and 8.4. Subspecialty names are recorded and displayed in English throughout.

**Operative logbook from the OR queue system.** The queue demo in this same repository (`../index.html`)
carries its case data inside the page, so the portfolio can read it directly over one fetch on GitHub Pages;
a JSON/CSV export or a real `GET /api/cases?scope=all&includeClosed=true` endpoint work the same way. Cases
de-duplicate on the queue's own case id, and **patient names are deliberately not imported** — date, HN, age,
sex, diagnosis, operation, side, subspecialty, room, anaesthesia, ASA, duration and the primary surgeon are.
HN is masked in the table until you ask to see it.

The queue system records only the primary surgeon — there is no assistant field yet — so the portfolio adds
the attendance layer: names found on the case are matched against the resident roster, and where that fails
the rotation calendar suggests who was on that service that day, in bulk if wanted. Roles are primary
surgeon, first/second assistant, observer, each with a staff sign-off. Case counts by role feed the per-year
requirements and print as a per-resident logbook. For full automation the queue system would need to record
attendees per case (`attendees: [{userId, role}]`, or a populated assistant list).

**Installable on phones, same code as the web app.** The page ships a manifest, icons and a service worker
scoped to its own folder, so it installs from Android's install button or iOS's *Add to Home Screen*, opens
full-screen and runs with no signal. A diagnostics panel in settings reports install state, offline
readiness, and why a queue connection failed. Wrapping the same files with Capacitor gives a store/MDM
build, whose native HTTP layer also sidesteps browser CORS and mixed-content rules.

**Recommended shape when the queue is intranet-only but people need the portfolio from anywhere:** keep two
zones and let the intranet matter only at the moment cases are pulled. Users fetch cases while on hospital
wifi (works today, nothing to open up), or — once a shared backend exists — a small one-way sync agent
inside the hospital pushes de-identified cases outward over HTTPS, so nothing inbound is exposed. Opening
the queue system through a VPN or reverse proxy is the option to avoid. If the portfolio's server sits
outside the hospital, set the patient-data level to "store nothing", so only diagnosis, operation, role and
date leave the building, with the queue's own case id as the reference back.

**Reaching an intranet-only queue system** depends on where this page is hosted: same server as the queue is
frictionless (same-origin, existing session, no CORS); another intranet host needs CORS with credentials; a
public HTTPS host such as GitHub Pages cannot reach an HTTP intranet service at all (mixed content) and must
fall back to file import. Data still lives per-device; sharing one record across residents and staff would
need a backend, most sensibly an endpoint on the queue system's own server, which the single `store` layer
here is ready to sync against.

**Logbook sync** runs per user and automatically — on load, five seconds after any edit, every ten minutes
while pending, and the moment the device comes back online — with a status chip in the header. The payload is
three fields: operation date, diagnosis, operation (plus role if the user opts in), and the settings dialog
shows the exact JSON before it is enabled. A reference server under `sync-server/` documents the contract and
strips anything outside the agreed field list a second time on the way in.

**The Royal College logbook stays the system of record** — it needs films and operative notes uploaded by the
resident. This page does not duplicate it: it prepares the data (cases pulled from the queue, roles tagged in
a couple of clicks), tracks which cases are still not entered there, and prints a tick-box worksheet of
exactly those, so the official entry is quick rather than reconstructed from memory.

**Sign-in and least-privilege access.** Three roles: residents see only their own portfolio, activities,
logbook and EPA (shared rosters stay visible); staff see every resident in order to assess and verify;
programme admins manage registries, rotations, requirements, EPA topics and connections. Actions that matter
— sign-in, verification, assessments, imports, plan generation — land in a local audit trail. The checks run
in the browser, so this separates views on a shared device rather than authenticating anyone; the permission
layer is isolated so it can move to server sessions without touching the screens.

**EPA assessment** tracks an entrustment level per topic against a per-year target, by subspecialty, with
history, a programme-wide matrix for staff, and assessment linked to a case where relevant. The topics
currently shipped are placeholders — the real list drops in via CSV (`code, title, subspecialty,
year1..year4`) or JSON and replaces them wholesale.

Tested in headless Chromium: `.pptx`/`.pdf` ingestion, classification, persistence, exports and reports.
Requires a browser with `DecompressionStream` (Chrome/Edge 80+, Safari 16.4+, Firefox 113+).

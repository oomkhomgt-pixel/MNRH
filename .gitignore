# ข้อมูลที่เซิร์ฟเวอร์อ้างอิงเขียนตอนรันทดสอบ ไม่ใช่ส่วนหนึ่งของโปรเจกต์
sync-server/data.json
sync-server/dataset.json

# skill ของบุคคลที่สามติดตั้งเองด้วย `npx skills add …` (ดู .claude/README.md)
# ไม่เก็บลง repo เพราะเป็นเครื่องมือของผู้พัฒนาแต่ละคน และมีขนาดหลายเมกะไบต์
.agents/
.claude/skills/

# playwright ที่ติดตั้งไว้รันชุดทดสอบ ไม่เก็บลง repo
# ไม่มี / ปิดท้าย เพราะบางครั้งเป็น symlink ไปยังที่ติดตั้งจริง ไม่ใช่โฟลเดอร์
portfolio/tests/node_modules

# dependency ของไปป์ไลน์แจ้งเตือน LINE — package-lock.json เก็บลง repo (เหมือน portfolio/tests/)
# เพื่อให้ `npm ci` บน GitHub Actions ได้เวอร์ชันเดียวกันทุกครั้ง
line-alert/node_modules

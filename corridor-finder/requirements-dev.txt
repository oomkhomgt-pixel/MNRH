# Development and batch work only; the Slicer module needs none of this.
# nibabel lets corridor_engine.nifti read a case without starting
# Slicer, which is what makes a sweep over a dataset or the blinded
# validation (DECISIONS.md section 5) practical.
-r requirements.txt
nibabel>=5.2,<6
